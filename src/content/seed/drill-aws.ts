import type { DrillItem } from '@shared/content';

import { cite } from '../sources';

/**
 * AWS bank: foundations (identity, network, data, cost) and the Bedrock/SageMaker
 * AI surface, written for engineers who have to defend an architecture in front of
 * a customer’s security and finance teams.
 *
 * These items are served only to users who selected AWS, so every question stands
 * on its own without reference to another cloud’s products.
 */
export const DRILL_AWS: DrillItem[] = [
  // ── IAM ──────────────────────────────────────────────────────────────────
  {
    id: 'aw.iam.eval_order',
    mode: 'drill',
    nodeIds: ['aws.iam', 'aws.organizations'],
    difficulty: 'deep',
    explanation:
      'Every AWS request starts denied. An explicit Deny found anywhere ends the evaluation immediately, then the organization guardrails and any permissions boundary cap what is even reachable, and only after that does an Allow in an identity or resource policy matter. People debug the wrong layer constantly: they widen an IAM policy when an SCP or a boundary was the thing saying no.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Put the stages of an AWS authorization decision in the order the evaluation actually applies them.',
      steps: [
        'Start from an implicit deny: nothing is permitted by default',
        'Look for an explicit Deny in any applicable policy, which settles the decision on the spot',
        'Apply the service control policies inherited from the organization, which can only subtract',
        'Apply any permissions boundary attached to the calling principal',
        'Look for an Allow in the identity-based or resource-based policies',
      ],
    },
  },
  {
    id: 'aw.iam.confused_deputy',
    mode: 'drill',
    nodeIds: ['aws.iam', 'sec.tenancy'],
    difficulty: 'deep',
    explanation:
      'When you hand a SaaS vendor a role to assume, the vendor’s account id is the only thing your trust policy checks by default, and that vendor assumes roles for all of its customers. Without a value that is unique to you, another of their customers can name your role ARN and the vendor will happily assume it on their behalf. An ExternalId condition in the trust policy is the fix, and it must be a value the vendor cannot let a customer choose.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Your product asks customers to create a cross-account IAM role your platform assumes. What stops one customer from tricking your platform into acting on another customer’s account?',
      choices: [
        { id: 'a', text: 'A trust policy that names your platform’s AWS account as principal', whyWrong: 'Every one of your customers writes that same condition, so it cannot tell one tenant from another. It is the condition the confused deputy relies on.' },
        { id: 'b', text: 'An sts:ExternalId condition carrying a value you assign per tenant' },
        { id: 'c', text: 'A permissions policy scoped to only the actions your platform calls', whyWrong: 'Permissions decide what the role can do once assumed. The gap here is who can cause it to be assumed at all.' },
        { id: 'd', text: 'An aws:MultiFactorAuthPresent condition on the AssumeRole call', whyWrong: 'MFA conditions describe human sessions. Your platform assumes the role unattended, so there is nobody to prompt.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.iam.least_priv',
    mode: 'drill',
    nodeIds: ['aws.iam', 'sec.zero_trust'],
    difficulty: 'core',
    explanation:
      'Reducing standing privilege is mostly about shortening credential lifetime and narrowing what a credential names, not about writing longer policy documents. Roles assumed on demand, conditions that pin the resource, and boundaries that cap delegation all shrink what a leaked credential is worth. Access Analyzer turns real CloudTrail activity into a tighter policy, which beats guessing.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which of these genuinely reduce standing privilege in an AWS account? Select all that apply.',
      choices: [
        { id: 'a', text: 'Replace IAM users holding access keys with roles assumed through federation' },
        { id: 'b', text: 'Generate least-privilege policies from observed CloudTrail activity with IAM Access Analyzer' },
        { id: 'c', text: 'Attach a permissions boundary so a delegated admin cannot grant more than the boundary allows' },
        { id: 'd', text: 'Rotate long-lived access keys every ninety days', whyWrong: 'Rotation shortens the window but the credential still sits somewhere for months at a time. It manages the risk rather than removing it.' },
        { id: 'e', text: 'Move broad permissions from inline policies into customer managed policies', whyWrong: 'That is a maintainability improvement. The same wildcard is still granted, just from a tidier document.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.iam.resource_vs_identity',
    mode: 'drill',
    nodeIds: ['aws.iam', 'aws.s3'],
    difficulty: 'core',
    explanation:
      'Inside one account, an Allow from either the identity policy or the resource policy is enough. Across accounts it is an AND: the calling principal’s account must allow the call and the resource owner’s policy must allow that principal. This is the single most common reason a cross-account bucket read fails while both sides insist their policy is correct.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A role in account A cannot read a bucket in account B, although the bucket policy names the role explicitly. What is the most likely gap?',
      choices: [
        { id: 'a', text: 'The bucket policy names the role ARN rather than a wildcard principal', whyWrong: 'Naming the principal is the correct, tighter form. A wildcard would widen exposure without supplying the Allow that is missing on the identity side.' },
        { id: 'b', text: 'The two accounts are not members of the same AWS organization', whyWrong: 'Organization membership is not a requirement. A resource policy can name a principal in any account, inside an organization or outside one.' },
        { id: 'c', text: 'The role’s identity policy in account A does not allow the action' },
        { id: 'd', text: 'S3 ignores resource policies when the caller is in another account', whyWrong: 'The opposite holds: for a cross-account call the resource policy is mandatory. It is simply not sufficient on its own.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.iam.boundary_vs_scp',
    mode: 'drill',
    nodeIds: ['aws.iam', 'aws.organizations'],
    difficulty: 'core',
    explanation:
      'Five different documents can all say "no" on an AWS request, and they exist for different owners. Knowing which one to reach for is what separates a five-minute fix from an afternoon of guessing, and it is also how you answer "can the account admin turn this off?" honestly in a security review.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each AWS policy type to the job it is actually for.',
      pairs: [
        { left: 'Service control policy', right: 'Org-level ceiling a member account admin cannot raise' },
        { left: 'Permissions boundary', right: 'Cap on what a delegated admin can grant to the principals they create' },
        { left: 'Role trust policy', right: 'Who is allowed to assume this role in the first place' },
        { left: 'Resource-based policy', right: 'Access granted by the resource owner, including to other accounts' },
        { left: 'Session policy', right: 'Further narrowing applied at the moment credentials are issued' },
      ],
    },
  },
  {
    id: 'aw.iam.role_session',
    mode: 'drill',
    nodeIds: ['aws.iam'],
    difficulty: 'intro',
    explanation:
      'Assuming a role does not copy a permanent credential around: STS mints a temporary access key, secret and session token that expire. That expiry is the security property, and it is why roles are the default answer to "how does this workload authenticate?" on every AWS design review.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What does an application actually receive when it assumes an IAM role?',
      choices: [
        { id: 'a', text: 'A permanent access key pair minted for the role when it was created', whyWrong: 'Roles hold no long-lived keys at all. That absence is precisely what separates a role from an IAM user.' },
        { id: 'b', text: 'A copy of the role’s policy documents, evaluated locally by the SDK', whyWrong: 'Policy evaluation happens on the AWS side at request time. A client never gets to decide its own authorization.' },
        { id: 'c', text: 'A presigned URL scoped to the specific service being called', whyWrong: 'Presigned URLs are an S3-style delegation mechanism and have nothing to do with how role credentials are issued.' },
        { id: 'd', text: 'Temporary STS credentials, including a session token, that expire' },
      ],
      correctId: 'd',
    },
  },

  // ── Federation & Roles Anywhere ──────────────────────────────────────────
  {
    id: 'aw.fed.github_oidc',
    mode: 'drill',
    nodeIds: ['aws.identity_federation', 'prod.cicd'],
    difficulty: 'core',
    explanation:
      'A CI system that holds an AWS access key is a secret sitting in a system with a large blast radius and a poor rotation story. Registering the CI provider as an IAM OIDC identity provider lets the pipeline exchange its short-lived workload token for role credentials, so nothing durable is stored anywhere.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer’s deployment pipeline stores an AWS access key as a CI secret. What do you propose instead?',
      choices: [
        { id: 'a', text: 'Register the CI provider as an IAM OIDC provider and assume a role' },
        { id: 'b', text: 'Move the key into Secrets Manager and have the pipeline fetch it', whyWrong: 'The pipeline still needs a credential to read Secrets Manager, so the problem has moved one hop rather than gone away.' },
        { id: 'c', text: 'Encrypt the key in the repository and decrypt it during the build', whyWrong: 'The decryption key now lives in CI instead, and the credential is still long-lived the moment it is decrypted.' },
        { id: 'd', text: 'Add an aws:SourceIp condition pinning the key to the runner IP range', whyWrong: 'Useful defense in depth, but hosted runners have wide and shifting egress ranges, and the key stays stealable and durable.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.fed.sub_condition',
    mode: 'drill',
    nodeIds: ['aws.identity_federation', 'aws.iam'],
    difficulty: 'edge',
    explanation:
      'Trusting an OIDC provider is only half the control. The trust policy also has to constrain the token claims, above all the subject, to the exact repository and branch or environment you meant. A wildcard on the subject means any repository on that CI platform, including one an attacker creates, presents a valid token and assumes your role.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A pipeline role trusts a CI platform’s OIDC provider with a condition matching any subject. Why does this fail a security review?',
      choices: [
        { id: 'a', text: 'OIDC tokens issued by CI providers are unsigned and easily forged', whyWrong: 'They are signed, and AWS verifies the signature against the provider’s published keys. The weakness is the missing claim constraint, not the cryptography.' },
        { id: 'b', text: 'Any account on that platform can mint a token the role accepts' },
        { id: 'c', text: 'The audience claim cannot be validated for third-party providers', whyWrong: 'The audience is validated and can be conditioned on. Subject is the claim that identifies which repository is actually calling.' },
        { id: 'd', text: 'Federated OIDC sessions are issued with no expiry on the credentials', whyWrong: 'A federated session is time bounded like any assumed role. Credential lifetime is not what fails the review here.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.fed.roles_anywhere',
    mode: 'drill',
    nodeIds: ['aws.identity_federation', 'idp.service_auth'],
    difficulty: 'deep',
    explanation:
      'IAM Roles Anywhere lets workloads that will never run on AWS obtain temporary role credentials by presenting an X.509 certificate issued by a certificate authority you register as a trust anchor. It is the right answer when the customer already runs a PKI for its data center fleet and does not want a per-server access key.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'On-premises batch servers need to write to S3 nightly. The customer runs an internal certificate authority and refuses to hold static AWS keys. What fits?',
      choices: [
        { id: 'a', text: 'One IAM user per server, each with a narrowly scoped write policy', whyWrong: 'That is the static-key model they rejected, multiplied by the fleet size and with a rotation problem on every host.' },
        { id: 'b', text: 'A proxy running in AWS that the servers call with a shared API key', whyWrong: 'You have invented a new credential to protect and a new component to keep available, and a shared secret is weaker than the certificates they already run.' },
        { id: 'c', text: 'IAM Roles Anywhere, with their internal CA as the trust anchor' },
        { id: 'd', text: 'An EC2 instance profile attached to each on-premises batch server', whyWrong: 'Instance profiles exist only for EC2. There is no metadata service on their own hardware to deliver credentials.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.fed.principal_match',
    mode: 'drill',
    nodeIds: ['aws.identity_federation', 'idp.service_auth'],
    difficulty: 'core',
    explanation:
      'Almost every "how do we authenticate?" question on AWS has a keyless answer, and the answer depends on where the caller runs. Getting this mapping straight is what lets you tell a customer, credibly, that no long-lived access key needs to exist anywhere in their estate.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each caller to the credential mechanism that avoids a stored access key.',
      pairs: [
        { left: 'An engineer using the console and CLI', right: 'IAM Identity Center with permission sets' },
        { left: 'Code running on EC2', right: 'Instance profile credentials from the instance metadata service' },
        { left: 'A pod in an EKS cluster', right: 'A service account mapped to an IAM role' },
        { left: 'A hosted CI job outside AWS', right: 'OIDC federation to an assumed role' },
        { left: 'A server in the customer’s own data center', right: 'IAM Roles Anywhere with an X.509 certificate' },
      ],
    },
  },
  {
    id: 'aw.fed.key_rotation',
    mode: 'drill',
    nodeIds: ['aws.identity_federation', 'sec.zero_trust'],
    difficulty: 'deep',
    explanation:
      'An access-key elimination program is mostly detection plus substitution: find where keys are being used, replace each use with the right keyless mechanism, then close the door with an organization-level control so new keys cannot be created. Skipping the last step means the count creeps back within two quarters.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'A customer wants zero long-lived IAM access keys across forty accounts. Which steps actually move them there? Select all that apply.',
      choices: [
        { id: 'a', text: 'Use IAM credential reports and CloudTrail to find which keys are still being used and by what' },
        { id: 'b', text: 'Replace each remaining use with role assumption via federation, instance profiles, or Roles Anywhere' },
        { id: 'c', text: 'Add a service control policy denying iam:CreateAccessKey so the population cannot regrow' },
        { id: 'd', text: 'Set a shorter maximum password age in the account password policy', whyWrong: 'Password policy governs console sign-in for IAM users, which is a separate credential type from programmatic access keys.' },
        { id: 'e', text: 'Store all existing keys in Secrets Manager with automatic rotation', whyWrong: 'Automated rotation keeps the keys alive and adds a dependency. The goal was to have no long-lived keys at all.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── Organizations & SCPs ─────────────────────────────────────────────────
  {
    id: 'aw.org.scp_not_grant',
    mode: 'drill',
    nodeIds: ['aws.organizations', 'aws.iam'],
    difficulty: 'intro',
    explanation:
      'A service control policy sets the ceiling on what principals in an account may do; it never grants anything by itself. An account with a permissive SCP and no IAM permissions can do nothing, and an account with a broad IAM policy still cannot exceed its SCP. Teams routinely attach an SCP expecting access to appear and then spend a day confused.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A platform team attaches an SCP allowing a set of services to an OU, and the workload accounts still cannot use them. Why?',
      choices: [
        { id: 'a', text: 'SCP changes take up to a day to propagate to every member account', whyWrong: 'SCP changes take effect essentially immediately. Waiting produces no grant that was never written in the first place.' },
        { id: 'b', text: 'The policy must attach to each account directly rather than to the OU', whyWrong: 'An attachment at the OU is inherited by every account beneath it. The attachment point is not what is missing.' },
        { id: 'c', text: 'SCPs stay inert until trusted access for IAM is enabled org-wide', whyWrong: 'All features must be enabled for SCPs to exist at all, but here the policy is attached and evaluating. The IAM grant is simply absent.' },
        { id: 'd', text: 'An SCP sets a ceiling only: IAM still has to grant the action' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.org.management_account',
    mode: 'drill',
    nodeIds: ['aws.organizations', 'sec.audit'],
    difficulty: 'edge',
    explanation:
      'Service control policies never restrict the organization’s management account, no matter where they are attached. That is why a mature landing zone keeps the management account empty of workloads: anything running there sits outside the guardrail everyone else is held to, and an auditor who knows this will ask about it directly.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'During a review you find production workloads deployed in the organization’s management account. What is the specific risk you raise?',
      choices: [
        { id: 'a', text: 'SCPs never apply there, so those workloads sit outside every guardrail' },
        { id: 'b', text: 'An organization trail cannot capture events from the management account', whyWrong: 'CloudTrail records the management account like any other. The exposure is the missing guardrail, not missing evidence.' },
        { id: 'c', text: 'Default service quotas are systematically lower in that account', whyWrong: 'Quotas are not lower there by design. Capacity is not the reason a landing zone keeps the account empty.' },
        { id: 'd', text: 'Consolidated billing hides the management account’s own spend', whyWrong: 'Its costs are itemized like any member account’s. Cost visibility is not what the auditor is asking about.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.org.region_deny',
    mode: 'drill',
    nodeIds: ['aws.organizations', 'sec.residency'],
    difficulty: 'deep',
    explanation:
      'Region restriction is normally written as a Deny on everything when aws:RequestedRegion is outside the approved list. The trap is that several AWS services have global or single-region control planes, IAM and CloudFront among them, so a naive blanket deny breaks account administration. The working pattern denies broadly and excludes those global actions with a NotAction.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A European customer wants an organization-wide control so resources cannot be created outside approved regions. What do you have to get right?',
      choices: [
        { id: 'a', text: 'Deny every action outside the approved regions with no NotAction carve-out', whyWrong: 'That breaks IAM, support and the other globally endpointed services, which is how these policies get rolled back within a day.' },
        { id: 'b', text: 'Deny on aws:RequestedRegion, excluding globally endpointed services' },
        { id: 'c', text: 'Opt out of the unwanted regions in each account’s region settings', whyWrong: 'Opt-out covers only the regions disabled by default. It leaves the enabled-by-default regions open and is not an enforceable org control.' },
        { id: 'd', text: 'Use a tag policy requiring an approved-region tag on every resource', whyWrong: 'Tag policies standardize tag keys and values after the fact. They cannot stop a resource being created somewhere.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.org.rollout_order',
    mode: 'drill',
    nodeIds: ['aws.organizations', 'prod.progressive'],
    difficulty: 'deep',
    explanation:
      'A restrictive SCP is a change with no gradual rollout of its own, so you build the gradient yourself: measure first from CloudTrail, prove it on one sacrificial OU, then widen. Teams that attach a deny at the root on a Friday learn about the dependency they forgot from an incident bridge.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Sequence a safe rollout of a new restrictive service control policy across an organization.',
      steps: [
        'Query CloudTrail to find who is currently performing the actions you intend to deny',
        'Work with the owners of any legitimate uses to migrate or carve an explicit exception',
        'Attach the policy to a sandbox OU and watch for access-denied errors',
        'Extend to non-production OUs and let a full release cycle run under it',
        'Attach at the root or the production OU and add an alarm on the deny signal',
      ],
    },
  },
  {
    id: 'aw.org.capabilities',
    mode: 'drill',
    nodeIds: ['aws.organizations', 'sec.zero_trust'],
    difficulty: 'core',
    explanation:
      'SCPs are the only control in AWS that a member-account administrator genuinely cannot remove, which makes them the right home for the handful of rules that must always hold. They are a blunt instrument though: they act on API actions and conditions, not on data content, and they do not touch the management account or service-linked roles.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which statements about service control policies are true? Select all that apply.',
      choices: [
        { id: 'a', text: 'An administrator in a member account cannot override or detach a policy attached above them' },
        { id: 'b', text: 'They can deny an action based on request conditions such as the target region' },
        { id: 'c', text: 'They do not restrict principals in the organization’s management account' },
        { id: 'd', text: 'They can grant a member account permissions its IAM policies do not include', whyWrong: 'SCPs only subtract. Nothing an SCP says can create an entitlement that IAM has not granted.' },
        { id: 'e', text: 'They can inspect the contents of an S3 object and block it if it contains PII', whyWrong: 'SCP evaluation happens at the API authorization layer and has no view of payload contents. That is a data-perimeter or scanning concern.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── VPC & networking ─────────────────────────────────────────────────────
  {
    id: 'aw.vpc.sg_vs_nacl',
    mode: 'drill',
    nodeIds: ['aws.vpc'],
    difficulty: 'intro',
    explanation:
      'Security groups are stateful and allow-only: permit a connection inbound and the return traffic is automatically permitted. Network ACLs are stateless and evaluated per direction in rule-number order, and they support deny, which is why they are used for coarse subnet-level blocks rather than for application access control.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Explain to a customer’s network team the practical difference between a security group and a network ACL.',
      choices: [
        { id: 'a', text: 'Security groups attach to subnets and NACLs attach to interfaces', whyWrong: 'It is the other way round. Security groups attach to elastic network interfaces and NACLs attach to subnets.' },
        { id: 'b', text: 'NACLs are stateful and security groups are ordered stateless lists', whyWrong: 'Reversed on both counts, and this exact inversion produces the classic "outbound works, replies never arrive" bug.' },
        { id: 'c', text: 'Security groups are stateful per interface; NACLs are stateless per subnet' },
        { id: 'd', text: 'They are equivalent filters under different names, so pick either one', whyWrong: 'They sit at different layers and behave differently on return traffic. Treating them as interchangeable is how the surprises start.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.vpc.nacl_ephemeral',
    mode: 'drill',
    nodeIds: ['aws.vpc', 'prod.incident'],
    difficulty: 'edge',
    explanation:
      'Because NACLs are stateless, a reply to an outbound connection arrives on a high-numbered ephemeral port and needs its own inbound allow rule. Someone tightening a NACL to "only 443 inbound" therefore breaks every outbound call the subnet makes, while the security groups look perfectly correct. The signature is connections that hang and then time out rather than being refused.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'After a network hardening change, instances in one subnet can open outbound connections but every response times out. Security groups are unchanged. Where do you look?',
      choices: [
        { id: 'a', text: 'The route table, which is likely missing its default route out', whyWrong: 'With no route the connection would never establish at all, rather than opening cleanly and then stalling on the reply.' },
        { id: 'b', text: 'VPC DNS resolution, which the hardening change may have redirected', whyWrong: 'A DNS failure surfaces as name resolution errors before any connection opens, not as an established connection that stalls.' },
        { id: 'c', text: 'The security group’s outbound rules, which now drop the replies', whyWrong: 'Security groups are stateful, so an allowed outbound connection already permits its return traffic. They were not changed anyway.' },
        { id: 'd', text: 'The subnet NACL, which needs an inbound allow for ephemeral ports' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.vpc.nat_cost',
    mode: 'drill',
    nodeIds: ['aws.vpc', 'aws.billing'],
    difficulty: 'deep',
    explanation:
      'A NAT gateway bills both an hourly charge and a per-gigabyte data processing charge on everything that passes through it, and that second component is what surprises people. Traffic from private subnets to S3 and DynamoDB can bypass NAT entirely through a gateway VPC endpoint, which is a route table entry and carries no per-hour or per-gigabyte endpoint charge of its own.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A data pipeline in private subnets pulls terabytes from S3 daily and the NAT gateway line item has become one of the largest on the bill. First move?',
      choices: [
        { id: 'a', text: 'Add a gateway VPC endpoint for S3 so the traffic never reaches NAT' },
        { id: 'b', text: 'Add a second NAT gateway so the volume is spread across both', whyWrong: 'Data processing is billed per gigabyte no matter how many gateways you run, so this adds hourly cost and saves nothing.' },
        { id: 'c', text: 'Move the pipeline into public subnets with public IPs on the hosts', whyWrong: 'It removes the NAT charge by putting the compute directly on the internet, which no security review will accept for a data pipeline.' },
        { id: 'd', text: 'Compress the objects so fewer bytes cross the NAT gateway', whyWrong: 'Worth doing on its own merits, but it only shrinks the symptom while every byte still takes the expensive path.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.vpc.nat_reduction',
    mode: 'drill',
    nodeIds: ['aws.vpc', 'aws.privatelink'],
    difficulty: 'deep',
    explanation:
      'The reliable ways to cut NAT spend all remove traffic from the NAT path rather than making NAT cheaper. Gateway endpoints take S3 and DynamoDB off it, interface endpoints take other AWS service traffic off it, and keeping NAT gateways in the same availability zone as the workloads avoids paying cross-zone transfer on top of the processing charge.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which changes reduce what a workload pays for NAT gateway data processing? Select all that apply.',
      choices: [
        { id: 'a', text: 'Route S3 and DynamoDB traffic through gateway VPC endpoints' },
        { id: 'b', text: 'Add interface endpoints for the other AWS services the workload calls most' },
        { id: 'c', text: 'Place a NAT gateway in each availability zone and route each subnet to its local one' },
        { id: 'd', text: 'Increase the instance size of the workload nodes', whyWrong: 'Instance size has no bearing on how many bytes traverse NAT. The same traffic is billed the same way.' },
        { id: 'e', text: 'Replace the NAT gateway with a NAT instance on a large EC2 type', whyWrong: 'It can shift cost in narrow cases but you take on patching, failover and a throughput ceiling you now own. Rarely the right advice for a customer.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.vpc.sg_reference',
    mode: 'drill',
    nodeIds: ['aws.vpc', 'aws.iam'],
    difficulty: 'core',
    explanation:
      'A security group rule can name another security group as its source instead of a CIDR block. The rule then follows the workload: instances added to the app tier are permitted automatically, and nothing has to be re-edited when subnets are resized or a new one is added. CIDR-based rules drift out of date the first time the network changes.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'The database tier’s security group allows port 5432 from the app subnet CIDR. Why do you suggest changing it?',
      choices: [
        { id: 'a', text: 'Narrow the rule to the individual private IPs of the app instances', whyWrong: 'Private IPs change whenever an instance is replaced, so this buys a maintenance burden and an outage on the next scaling event.' },
        { id: 'b', text: 'Reference the app tier’s security group as the source instead' },
        { id: 'c', text: 'Move the port 5432 rule onto the subnet NACL where it is coarser', whyWrong: 'NACLs are stateless and subnet-wide. Application-level access control belongs in security groups, not at the subnet edge.' },
        { id: 'd', text: 'Nothing, since CIDR-based source rules are the recommended pattern', whyWrong: 'They work but they are brittle. Anything else placed in that CIDR, now or after the next subnet change, inherits database access.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.vpc.subnet_routing',
    mode: 'drill',
    nodeIds: ['aws.vpc'],
    difficulty: 'intro',
    explanation:
      'There is no "public" checkbox on a subnet. A subnet is public precisely when its route table has a route to an internet gateway; everything else about it is identical. This is the first thing to verify when an instance that should be reachable is not.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What makes a VPC subnet public rather than private?',
      choices: [
        { id: 'a', text: 'The instances inside it have been assigned public IP addresses', whyWrong: 'A public IP with no internet gateway route is unreachable. The address is necessary but the route is what defines the subnet.' },
        { id: 'b', text: 'Its security groups permit inbound traffic from any internet address', whyWrong: 'Security groups filter traffic that can already reach the interface. They never create reachability that the routing does not.' },
        { id: 'c', text: 'Its route table carries a route to an internet gateway' },
        { id: 'd', text: 'It was allocated a CIDR block from a publicly routable range', whyWrong: 'VPC subnets use private addressing regardless of the block you picked. The CIDR does not decide internet reachability.' },
      ],
      correctId: 'c',
    },
  },

  // ── PrivateLink & endpoints ──────────────────────────────────────────────
  {
    id: 'aw.pl.deliver_software',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'sec.tenancy'],
    difficulty: 'deep',
    explanation:
      'PrivateLink is the mechanism for delivering a service into someone else’s VPC without joining the two networks. You put your service behind a network load balancer, publish it as an endpoint service, and the customer creates an interface endpoint that appears as a private IP inside their own address space. No routes are exchanged, and overlapping CIDRs stop being a conversation.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Your product runs in your AWS account and enterprise customers insist it must be reachable without traversing the public internet. What do you build?',
      choices: [
        { id: 'a', text: 'A VPC peering connection to each customer, with routes on both sides', whyWrong: 'Peering joins two whole networks, demands non-overlapping CIDRs and route edits on both sides, and grants a vendor far more reach than any customer wants to.' },
        { id: 'b', text: 'A site-to-site VPN tunnel terminated in each customer environment', whyWrong: 'Heavy to operate at any real customer count, and it still joins networks rather than exposing the one service they asked for.' },
        { id: 'c', text: 'A public API endpoint locked down by a per-customer IP allowlist', whyWrong: 'Traffic still crosses the internet, which is the exact objection raised. Allowlists also break the day a customer changes egress addresses.' },
        { id: 'd', text: 'A PrivateLink endpoint service behind a network load balancer' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.pl.gateway_vs_interface',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'aws.vpc'],
    difficulty: 'core',
    explanation:
      'Gateway endpoints exist for S3 and DynamoDB only. They work by adding a prefix-list route to your route table and carry no endpoint charge, but they are reachable only from inside the VPC they are attached to. Interface endpoints put an actual elastic network interface with a private IP in your subnet, work for most AWS services, and can be reached from on premises over Direct Connect or VPN.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'On-premises jobs reaching AWS over Direct Connect need private access to S3. Why is a gateway endpoint not the answer here?',
      choices: [
        { id: 'a', text: 'They are route table entries, so nothing outside the VPC can use them' },
        { id: 'b', text: 'Gateway endpoints cover DynamoDB only and never carried S3 traffic', whyWrong: 'S3 is one of the two services gateway endpoints exist for. Reachability from on premises is the constraint here, not service coverage.' },
        { id: 'c', text: 'Gateway endpoints accept no endpoint policy, so access cannot be scoped', whyWrong: 'They do support endpoint policies, and those policies are commonly used to restrict which buckets are reachable. That is not the limitation.' },
        { id: 'd', text: 'Gateway endpoints bill per gigabyte, which is prohibitive at this volume', whyWrong: 'Gateway endpoints carry no hourly or per-gigabyte endpoint charge at all. Cost is not the reason to reject them.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.pl.private_dns',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'prod.incident'],
    difficulty: 'edge',
    explanation:
      'An interface endpoint only intercepts traffic if the service’s public DNS name resolves to the endpoint’s private IP. That is what enabling private DNS on the endpoint does, and it depends on DNS hostnames and DNS support being enabled in the VPC. With it off, the SDK resolves the public name, the request goes out through NAT, and everything works while quietly defeating the entire private-connectivity design.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'An interface endpoint for a service is provisioned and healthy, yet traffic is still leaving through the NAT gateway. What is the usual cause?',
      choices: [
        { id: 'a', text: 'The endpoint was placed in a different zone from the calling workload', whyWrong: 'A zone mismatch shows up as failures or cross-zone charges on the endpoint path, not as a clean fallback onto the public one.' },
        { id: 'b', text: 'Private DNS is off, so the public hostname still resolves publicly' },
        { id: 'c', text: 'The endpoint policy is too restrictive and is rejecting the calls', whyWrong: 'A restrictive endpoint policy returns access-denied on the endpoint path. It never reroutes a request out to the internet.' },
        { id: 'd', text: 'The endpoint’s security group does not allow inbound traffic on 443', whyWrong: 'That produces connection failures against the endpoint, not silent success over NAT with everything appearing healthy.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.pl.direction',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'sec.zero_trust'],
    difficulty: 'core',
    explanation:
      'PrivateLink is deliberately one-way: the consumer initiates to the provider, and the provider gets no route back into the consumer VPC. That asymmetry is the security selling point, and it is also a design constraint. If your product needs to push events to the customer, you need a second mechanism such as the customer polling you, or the customer publishing their own endpoint service for you to consume.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Your PrivateLink-delivered service now needs to send webhook callbacks into the customer VPC. What is the honest answer?',
      choices: [
        { id: 'a', text: 'Enable bidirectional mode on the endpoint service configuration', whyWrong: 'No such setting exists. Inventing a feature in front of a customer is how you lose the rest of the review.' },
        { id: 'b', text: 'Add a return route through the endpoint in the customer route table', whyWrong: 'The endpoint is not a router and exchanges no routes at all. That mental model comes from peering and does not transfer.' },
        { id: 'c', text: 'PrivateLink is one-way, so callbacks need a separate path back' },
        { id: 'd', text: 'Resolve the endpoint’s DNS name from your side to reach their network', whyWrong: 'That name resolves only inside the consumer VPC, and the provider has no reachability into that address space.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.pl.overlap',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'aws.transit'],
    difficulty: 'core',
    explanation:
      'VPC peering and Transit Gateway both do IP routing, so both require non-overlapping CIDRs between the connected networks. Enterprises that grew by acquisition frequently have 10.0.0.0/16 in three places and cannot renumber on your timeline. PrivateLink sidesteps this because nothing is routed: the endpoint is an interface with an address from the consumer’s own space.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer’s VPC uses the same CIDR block as yours and renumbering is off the table this year. Which connectivity option still works?',
      choices: [
        { id: 'a', text: 'VPC peering, using more specific routes to disambiguate the overlap', whyWrong: 'Peering rejects overlapping CIDRs outright at creation time. No amount of route specificity makes the connection come up.' },
        { id: 'b', text: 'Transit Gateway, whose route tables can hold overlapping prefixes', whyWrong: 'Transit Gateway is still IP routing. Two attachments advertising the same prefix leave the destination genuinely ambiguous.' },
        { id: 'c', text: 'A site-to-site VPN, since the tunnel encapsulates the addressing', whyWrong: 'Encapsulation does not resolve the ambiguity at the far end. You end up in NAT gymnastics that nobody wants to own.' },
        { id: 'd', text: 'PrivateLink, since the endpoint takes a consumer VPC address' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.pl.endpoint_policy',
    mode: 'drill',
    nodeIds: ['aws.privatelink', 'sec.pii'],
    difficulty: 'deep',
    explanation:
      'Stopping data from being copied to an attacker-controlled S3 bucket is a data-perimeter problem, and it takes controls on both the network path and the identity path. VPC endpoint policies restrict which buckets are reachable through the endpoint, aws:PrincipalOrgID conditions on the bucket side keep outsiders out, and denying the public S3 path forces all traffic through the endpoint where the policy applies.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'A regulated customer wants to guarantee that compute in their VPC cannot write to any S3 bucket outside their organization. Which controls contribute? Select all that apply.',
      choices: [
        { id: 'a', text: 'A VPC endpoint policy on the S3 endpoint that allows only buckets owned by their organization' },
        { id: 'b', text: 'An SCP denying S3 actions when aws:ResourceOrgID does not match their organization' },
        { id: 'c', text: 'Removing the internet egress path so S3 is only reachable through the endpoint' },
        { id: 'd', text: 'Enabling default encryption with a customer managed key on their own buckets', whyWrong: 'Encryption at rest protects data in their buckets. It says nothing about whether a process can write elsewhere.' },
        { id: 'e', text: 'Turning on S3 Block Public Access across the account', whyWrong: 'That stops their buckets being exposed publicly. It does not constrain outbound writes to somebody else’s bucket.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.pl.publish_order',
    mode: 'drill',
    nodeIds: ['aws.privatelink'],
    difficulty: 'core',
    explanation:
      'Publishing a service over PrivateLink has a fixed shape, and the acceptance step is the one people forget when they write the runbook. The consumer request sits pending until the provider allows the consumer principal and approves the connection, which is exactly the control point you want when onboarding a named customer.',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Order the steps to expose your service to a customer VPC over PrivateLink.',
      steps: [
        'Put the service behind a network load balancer in your own VPC',
        'Create a VPC endpoint service configuration pointing at that load balancer',
        'Add the customer’s account or role as an allowed principal on the endpoint service',
        'The customer creates an interface endpoint for your service name in their subnets',
        'Approve the pending endpoint connection and confirm the customer can resolve and reach it',
      ],
    },
  },

  // ── Transit Gateway & hybrid ─────────────────────────────────────────────
  {
    id: 'aw.tgw.mesh_scaling',
    mode: 'drill',
    nodeIds: ['aws.transit', 'aws.vpc'],
    difficulty: 'core',
    explanation:
      'VPC peering is point to point and non-transitive: to connect n VPCs to each other you need n(n-1)/2 peerings and a route entry per destination in every route table. It stays fine at four VPCs and becomes unmanageable somewhere around a dozen. Transit Gateway replaces the mesh with a hub that each VPC attaches to once.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer with twelve VPCs wants them all able to reach each other. Why do you steer them away from a peering mesh?',
      choices: [
        { id: 'a', text: 'Peering is non-transitive: full reach needs sixty-six connections' },
        { id: 'b', text: 'Peering connections cannot be made between two different accounts', whyWrong: 'Cross-account peering works fine and is common. The obstacle is the connection and route count, not the account boundary.' },
        { id: 'c', text: 'Peering charges a per-attachment hourly fee larger than the hub’s', whyWrong: 'Peering has no hourly attachment charge, and Transit Gateway does. Cost usually favors peering for a small number of VPCs.' },
        { id: 'd', text: 'Peered VPCs cannot reference each other’s security groups at all', whyWrong: 'Security group referencing across a peering connection is supported within a region. That is not the scaling problem.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.tgw.route_tables',
    mode: 'drill',
    nodeIds: ['aws.transit', 'sec.tenancy'],
    difficulty: 'deep',
    explanation:
      'A single Transit Gateway route table connects everything to everything, which is rarely what a security team signed off. Multiple route tables with deliberate attachment associations and propagations give you segments: production and non-production can each reach shared services without being able to reach each other. This is the design conversation, and it is easier to get right before the first attachment than after fifty.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Production and development VPCs both attach to one Transit Gateway and can now reach each other, which security has flagged. What is the fix?',
      choices: [
        { id: 'a', text: 'Add deny entries for the development prefixes to the gateway route table', whyWrong: 'Transit Gateway route tables hold routes, not deny rules. Segmentation is expressed entirely through what you choose to propagate.' },
        { id: 'b', text: 'Give each segment its own route table and propagate selectively' },
        { id: 'c', text: 'Attach security groups to the Transit Gateway attachments themselves', whyWrong: 'Attachments do not take security groups. Filtering happens inside the VPCs or in an inspection appliance you route through.' },
        { id: 'd', text: 'Detach development from the gateway and peer it directly instead', whyWrong: 'Workable for two VPCs, but it abandons the hub the moment a third appears and still expresses no segmentation policy.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.tgw.dx',
    mode: 'drill',
    nodeIds: ['aws.transit', 'del.pilot_to_prod'],
    difficulty: 'core',
    explanation:
      'Direct Connect gives dedicated bandwidth and predictable latency, but provisioning a circuit runs on a telecom timeline of weeks to months. Site-to-site VPN over the internet is available the same day. The professional answer on a pilot is almost always to start on VPN, order the circuit in parallel, and design so the cutover is a routing change rather than a redesign.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'The customer wants Direct Connect for a pilot starting in three weeks. How do you handle it?',
      choices: [
        { id: 'a', text: 'Delay the pilot start date until the Direct Connect circuit is live', whyWrong: 'You have traded a solvable connectivity constraint for months of lost momentum, which is usually what kills a pilot outright.' },
        { id: 'b', text: 'Run the pilot on public endpoints and add private access afterwards', whyWrong: 'Retrofitting private connectivity once the security review has passed on a public design means running that review twice.' },
        { id: 'c', text: 'Start on site-to-site VPN and order the circuit in parallel' },
        { id: 'd', text: 'Commit to a Direct Connect delivery date inside the three-week window', whyWrong: 'Lead times belong to the carrier and the colocation facility. Promising a date you do not control is the classic overpromise.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.tgw.cost',
    mode: 'drill',
    nodeIds: ['aws.transit', 'aws.billing'],
    difficulty: 'core',
    explanation:
      'Transit Gateway charges per attachment-hour and per gigabyte processed, on top of any data transfer. For two VPCs exchanging large volumes in the same region, peering has no attachment charge and no per-gigabyte processing charge of its own, so it is materially cheaper. Transit Gateway earns its price once the number of connections, or the need for segmentation and transitive routing, makes the mesh untenable.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Two VPCs in one region move heavy traffic between them and nothing else needs connecting. Which topology do you recommend on cost grounds?',
      choices: [
        { id: 'a', text: 'Transit Gateway now, since a third and fourth VPC will arrive eventually', whyWrong: 'Speculative architecture that bills every attachment-hour and every gigabyte from day one. Add the hub when a third participant actually appears.' },
        { id: 'b', text: 'PrivateLink between the two VPCs, each exposing its services privately', whyWrong: 'The right tool for exposing one service across a trust boundary, and the wrong one for bulk traffic between two networks you already own.' },
        { id: 'c', text: 'Merge both workloads into a single VPC and drop the connection', whyWrong: 'Cheapest on paper and sometimes correct, but it collapses the blast radius and account boundaries that justified two VPCs.' },
        { id: 'd', text: 'VPC peering, with no attachment or per-gigabyte processing charge' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.tgw.connectivity_match',
    mode: 'drill',
    nodeIds: ['aws.transit', 'aws.privatelink'],
    difficulty: 'core',
    explanation:
      'Five AWS connectivity primitives, five different problems. Most bad network designs come from picking one of these by familiarity and then bending the requirement to fit it, usually peering when the requirement was really "expose one service to a party we do not fully trust".',
    citations: cite('awsPrivatelink'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each AWS connectivity option to the problem it is the right answer for.',
      pairs: [
        { left: 'VPC peering', right: 'Two networks you own, non-overlapping CIDRs, low count' },
        { left: 'Transit Gateway', right: 'Many VPCs plus hybrid, with routing segments between them' },
        { left: 'PrivateLink', right: 'Expose one service across a trust boundary, no routes shared' },
        { left: 'Direct Connect', right: 'Dedicated bandwidth and predictable latency from a data center' },
        { left: 'Site-to-site VPN', right: 'Encrypted hybrid reachability you can turn up the same day' },
      ],
    },
  },

  // ── Landing zone ─────────────────────────────────────────────────────────
  {
    id: 'aw.lz.control_types',
    mode: 'drill',
    nodeIds: ['aws.landing_zone', 'sec.audit'],
    difficulty: 'core',
    explanation:
      'Guardrails come in three flavors and customers conflate them constantly. Preventive controls stop the API call, detective controls tell you afterwards that something drifted, and proactive controls block a non-compliant resource before deployment. A control framework made only of detective rules is a report, not a guardrail.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each guardrail mechanism to the kind of control it provides.',
      pairs: [
        { left: 'Service control policy', right: 'Preventive: the API call is denied outright' },
        { left: 'AWS Config rule', right: 'Detective: flags a resource that is already non-compliant' },
        { left: 'CloudFormation hook', right: 'Proactive: blocks the resource before it is provisioned' },
        { left: 'Organization CloudTrail', right: 'Evidence: the immutable record of who called what' },
      ],
    },
  },
  {
    id: 'aw.lz.log_archive',
    mode: 'drill',
    nodeIds: ['aws.landing_zone', 'sec.audit'],
    difficulty: 'intro',
    explanation:
      'Audit logs stored in the account that generated them can be deleted by whoever compromised that account. A landing zone therefore ships an organization trail writing to a dedicated log archive account that workload administrators have no write access to, usually with object lock. That separation is what makes the log an evidence artifact rather than a convenience.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Why does a well-built AWS landing zone put CloudTrail logs in a separate account rather than in each workload account?',
      choices: [
        { id: 'a', text: 'So whoever controls a workload account cannot alter or delete its trail' },
        { id: 'b', text: 'Because CloudTrail is not permitted to write to a same-account bucket', whyWrong: 'It is permitted, and plenty of accounts do exactly that. Centralizing is a deliberate integrity decision, not a technical constraint.' },
        { id: 'c', text: 'To consolidate storage and earn a lower effective per-gigabyte rate', whyWrong: 'S3 is priced per gigabyte no matter which account owns the bucket, and volume tiers already aggregate under consolidated billing.' },
        { id: 'd', text: 'Because an organization trail cannot be queried across accounts', whyWrong: 'Central query convenience is a genuine benefit, but what auditors actually care about is that the record cannot be tampered with.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.lz.account_boundaries',
    mode: 'drill',
    nodeIds: ['aws.landing_zone', 'sec.tenancy'],
    difficulty: 'deep',
    explanation:
      'An AWS account is the strongest isolation boundary the platform offers, and it is the unit for several things people forget: service quotas, most default resource limits, consolidated but attributable billing, and the blast radius of a credential compromise. What it is not is a free lunch, since every account needs the baseline of networking, logging and access wired in, which is exactly why account vending exists.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'What does putting a workload in its own AWS account actually buy you? Select all that apply.',
      choices: [
        { id: 'a', text: 'Its own service quotas, so a noisy workload cannot exhaust another’s limits' },
        { id: 'b', text: 'A hard blast-radius boundary for a compromised credential or a runaway automation' },
        { id: 'c', text: 'Cost attribution that does not depend on everyone tagging correctly' },
        { id: 'd', text: 'Automatic network isolation from every other account', whyWrong: 'Accounts are isolated by default at the IAM layer, but VPCs can be peered, attached or endpoint-connected. Network isolation is a separate design decision.' },
        { id: 'e', text: 'Lower per-unit pricing through separate billing', whyWrong: 'Volume tiers aggregate across the organization under consolidated billing. Splitting accounts does not earn a better rate, and can lose one.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.lz.retrofit',
    mode: 'drill',
    nodeIds: ['aws.landing_zone', 'del.pilot_to_prod'],
    difficulty: 'edge',
    explanation:
      'Retrofitting governance onto accounts that already run production is mostly a discovery and sequencing problem. Enrolling an existing account applies the baseline controls to resources built with different assumptions, so the first pass should be detective only: measure what would break, fix or exempt it, then switch the preventive controls on. Big-bang enrollment of a live account is how a platform team loses the trust of its workload teams.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer has thirty pre-existing accounts and wants them brought under a governed landing zone. What do you do first?',
      choices: [
        { id: 'a', text: 'Enable the full preventive and detective control set across all thirty', whyWrong: 'Every deny lands on a live workload at the same moment, and you have no way to tell which control broke which application.' },
        { id: 'b', text: 'Enroll with detective controls first, then enforce per OU' },
        { id: 'c', text: 'Create new governed accounts and migrate every workload across', whyWrong: 'Sometimes right for a handful of accounts, but as a blanket plan it is a multi-year migration when the customer wanted governance this quarter.' },
        { id: 'd', text: 'Govern only newly vended accounts and leave the thirty as they are', whyWrong: 'That leaves the entire existing estate ungoverned, which is exactly the population the auditor is asking about.' },
      ],
      correctId: 'b',
    },
  },

  // ── Compute choice ───────────────────────────────────────────────────────
  {
    id: 'aw.compute.lambda_limits',
    mode: 'drill',
    nodeIds: ['aws.compute_choice'],
    difficulty: 'intro',
    explanation:
      'Lambda has a hard maximum execution duration measured in minutes, so any single invocation that must run for hours is the wrong shape for it. The two honest options are to break the work into a Step Functions state machine of short tasks, or to run it as a container task on Fargate where duration is not the constraint.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A nightly reconciliation job takes about two hours and the team wants it on Lambda. What do you tell them?',
      choices: [
        { id: 'a', text: 'Raise the function timeout with a Service Quotas increase request', whyWrong: 'The maximum invocation duration is a hard platform limit rather than an adjustable quota. Support cannot move it for you.' },
        { id: 'b', text: 'Have the function re-invoke itself before the timeout to continue', whyWrong: 'Recursive continuation makes checkpointing, idempotency and failure handling your problem, which is the work Step Functions already does.' },
        { id: 'c', text: 'It exceeds the invocation limit: use Step Functions or ECS' },
        { id: 'd', text: 'Allocate the maximum memory so the extra CPU finishes it in time', whyWrong: 'More memory buys more CPU and can help a compute-bound job, but you would be betting the job never grows past the ceiling again.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.compute.handback',
    mode: 'drill',
    nodeIds: ['aws.compute_choice', 'del.handover'],
    difficulty: 'deep',
    explanation:
      'The runtime you choose is a runtime the customer’s team has to operate after you leave. A four-person team with no platform engineer will not keep a Kubernetes cluster patched, upgraded and secured, and the thing they inherit will rot. Choosing the most managed runtime the workload permits is a delivery decision as much as a technical one.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'You are handing a containerized API to a four-person product team with no platform engineer. Which runtime do you leave them on?',
      choices: [
        { id: 'a', text: 'EKS with managed node groups, so AWS handles the AMI patching', whyWrong: 'Managed node groups reduce the toil but leave version upgrades, add-on compatibility and the Kubernetes learning curve with a team that has nobody to own them.' },
        { id: 'b', text: 'EC2 in an auto scaling group with the deployment script you wrote', whyWrong: 'They inherit AMI patching, capacity planning and a bespoke deploy path that nobody outside your engagement understands.' },
        { id: 'c', text: 'Whichever runtime the team already knows, even self-managed Kubernetes', whyWrong: 'Familiarity matters, but self-managed Kubernetes carries an operational floor this team cannot staff no matter how familiar it feels.' },
        { id: 'd', text: 'ECS on Fargate, with no nodes or control plane for them to patch' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.compute.eks_when',
    mode: 'drill',
    nodeIds: ['aws.compute_choice', 'aws.eks'],
    difficulty: 'core',
    explanation:
      'EKS is justified when you need the Kubernetes ecosystem itself, not merely the ability to run containers: operators, custom resources, service mesh, GPU scheduling with device plugins, or portability commitments to another environment. If the requirement is just "run this container reliably", the managed container service is fewer moving parts and fewer upgrades.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What actually justifies EKS over a simpler managed container service on AWS?',
      choices: [
        { id: 'a', text: 'It depends on Kubernetes itself: operators, custom resources, device plugins' },
        { id: 'b', text: 'The team has standardized on containers as their deployment artifact', whyWrong: 'Containers run perfectly well on the simpler managed service. Wanting containers is not on its own a reason to take on a control plane.' },
        { id: 'c', text: 'The workload has to scale out automatically under variable load', whyWrong: 'Every AWS container runtime autoscales, including the simpler managed one. Autoscaling does not differentiate them.' },
        { id: 'd', text: 'Kubernetes works out cheaper per unit of compute at large scale', whyWrong: 'Compute rates are broadly comparable, and on top you add cluster operations, an upgrade cadence and usually a platform engineer.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.compute.lambda_streaming',
    mode: 'drill',
    nodeIds: ['aws.compute_choice', 'ai.latency'],
    difficulty: 'edge',
    explanation:
      'Token-by-token streaming needs a path that forwards partial responses as they are produced, and a request/response integration buffers the whole body instead. Lambda response streaming works through a function URL or an application load balancer, which is why a streaming chat endpoint on Lambda is usually fronted by one of those rather than by a buffering API integration.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A chat endpoint on Lambda has to stream model output to the browser, but users only see the reply appear all at once at the end. What is the structural problem?',
      choices: [
        { id: 'a', text: 'Lambda handlers can only return a single buffered response body', whyWrong: 'Lambda supports response streaming directly from the handler. The buffering is happening in the integration sitting in front of it.' },
        { id: 'b', text: 'The front door buffers the body: use a function URL or an ALB' },
        { id: 'c', text: 'The browser client waits for the whole body before it renders', whyWrong: 'Possible in a badly written client, but one that already handles server-sent events renders partials the moment they actually arrive.' },
        { id: 'd', text: 'The model provider’s API does not stream tokens as they are produced', whyWrong: 'Streaming APIs are standard and worth confirming, but a buffering front door is by far the more common cause of this exact symptom.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.compute.runtime_match',
    mode: 'drill',
    nodeIds: ['aws.compute_choice'],
    difficulty: 'intro',
    explanation:
      'Compute choice on AWS is mostly a question of how much of the stack you are willing to own. Matching workload shape to runtime up front avoids the two classic mistakes: putting a long-running stateful process on a function runtime, and standing up a cluster to run one cron job.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each workload to the AWS runtime that fits it with the least operational overhead.',
      pairs: [
        { left: 'Event-driven glue that runs for seconds', right: 'Lambda' },
        { left: 'Long-running container API, small team', right: 'ECS on Fargate' },
        { left: 'Platform with operators and custom resources', right: 'EKS' },
        { left: 'Licensed software needing a specific host', right: 'EC2' },
      ],
    },
  },

  // ── EKS ──────────────────────────────────────────────────────────────────
  {
    id: 'aw.eks.irsa',
    mode: 'drill',
    nodeIds: ['aws.eks', 'aws.identity_federation'],
    difficulty: 'core',
    explanation:
      'Mapping a Kubernetes service account to an IAM role, whether through IAM roles for service accounts or EKS Pod Identity, gives each pod its own AWS identity. Without it, pods fall back to the node instance profile, which means every workload on that node shares one set of permissions and CloudTrail cannot tell you which of them made a call.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'In an EKS cluster, why bind IAM roles to Kubernetes service accounts rather than granting the node role what the pods need?',
      choices: [
        { id: 'a', text: 'Node instance roles cannot be granted access to most AWS services', whyWrong: 'They can be granted almost anything, which is exactly why the shortcut is tempting and why over-permissioned node roles are so common.' },
        { id: 'b', text: 'It removes the need to run an OIDC provider on the cluster at all', whyWrong: 'IAM roles for service accounts depends on the cluster OIDC provider. That is the mechanism doing the work, not something avoided.' },
        { id: 'c', text: 'Each workload gets its own identity, so permissions scope per pod' },
        { id: 'd', text: 'It cuts the number of AssumeRole calls the cluster makes to STS', whyWrong: 'Per-pod credentials mean more STS activity, not less. What you buy is isolation and attribution, not a reduction in call volume.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.eks.node_role_blast',
    mode: 'drill',
    nodeIds: ['aws.eks', 'sec.tenancy'],
    difficulty: 'core',
    explanation:
      'Anything on the node can reach the instance metadata service and take the node role’s credentials, so a broad node role means one compromised container has whatever the busiest workload on that node was granted. Blocking pod access to the metadata service, or setting the hop limit so containers cannot reach it, is the companion control to per-pod roles.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A shared EKS cluster gives its node role S3 and DynamoDB access "so the pods can work". What is the exposure?',
      choices: [
        { id: 'a', text: 'The node role’s credentials expire and pods lose access during rotation', whyWrong: 'Instance profile credentials rotate transparently and applications never notice. Availability is not what is wrong with this design.' },
        { id: 'b', text: 'Kubernetes network policy already blocks pods from reaching AWS APIs', whyWrong: 'Network policy is not enforced by default, and even where it is, the metadata endpoint is routinely left reachable from pods.' },
        { id: 'c', text: 'CloudTrail attributes every call to an individual pod, which is noisy', whyWrong: 'The opposite happens: every call looks identical because one role made them all. Losing attribution is part of the exposure.' },
        { id: 'd', text: 'Any pod can take those credentials from the metadata service' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.eks.upgrade_order',
    mode: 'drill',
    nodeIds: ['aws.eks', 'prod.progressive'],
    difficulty: 'core',
    explanation:
      'Kubernetes versions leave standard support on a schedule, so an EKS cluster is on a permanent upgrade cadence whether or not the team planned for one. The order matters: deprecated API usage has to be found and fixed before the control plane moves, and the control plane goes before the nodes because a newer node than control plane is unsupported.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Sequence an EKS cluster version upgrade so it does not take an application down.',
      steps: [
        'Read the version’s deprecated and removed API list and scan workload manifests for them',
        'Check that add-ons and controllers such as the CNI, CoreDNS and ingress support the target version',
        'Upgrade a non-production cluster and run the application test suite against it',
        'Upgrade the production control plane',
        'Roll the node groups to the matching version, draining nodes gradually',
      ],
    },
  },
  {
    id: 'aw.eks.cost',
    mode: 'drill',
    nodeIds: ['aws.eks', 'del.tco'],
    difficulty: 'deep',
    explanation:
      'The per-cluster control plane charge is the smallest and most visible part of what EKS costs. The real spend is idle node capacity held for scheduling headroom, the load balancers and NAT the cluster provisions, the observability pipeline, and the engineer-time of the upgrade treadmill. Quoting only the control plane fee to a customer sets up a bad conversation later.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'When a customer asks what EKS will really cost them, which items belong in the honest answer? Select all that apply.',
      choices: [
        { id: 'a', text: 'Node capacity held idle as scheduling headroom, not just the nodes running pods' },
        { id: 'b', text: 'Ongoing engineer time for version upgrades and add-on compatibility' },
        { id: 'c', text: 'Load balancers, NAT and log ingestion the cluster brings with it' },
        { id: 'd', text: 'A per-pod licensing charge for Kubernetes', whyWrong: 'Kubernetes is open source and there is no per-pod license. Inventing a cost line damages your credibility on the ones that are real.' },
        { id: 'e', text: 'Data transfer between pods on the same node', whyWrong: 'Traffic within a node is not billed. Cross-availability-zone traffic is the one worth watching.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── S3 ───────────────────────────────────────────────────────────────────
  {
    id: 'aw.s3.small_object_lifecycle',
    mode: 'drill',
    nodeIds: ['aws.s3', 'aws.billing'],
    difficulty: 'edge',
    explanation:
      'Lifecycle transitions are billed per object moved, archive classes add per-object metadata overhead, and they impose a minimum billable storage duration. Move a hundred million tiny objects to an archive class and the transition requests plus the overhead can exceed the storage you were trying to save. Aggregating small objects before archiving is the fix.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A team plans a lifecycle rule moving a hundred million small log objects to an archive class after thirty days. What do you flag?',
      choices: [
        { id: 'a', text: 'Transition requests and per-object overhead exceed the storage saved' },
        { id: 'b', text: 'Lifecycle rules cannot select objects below a minimum size threshold', whyWrong: 'The rule applies to objects of any size. What makes this plan bad is the economics, not a filtering restriction.' },
        { id: 'c', text: 'Objects in an archive class can no longer be listed from the bucket', whyWrong: 'Listing keeps working; it is retrieval that changes shape. That is not what makes this rule a mistake.' },
        { id: 'd', text: 'Thirty days is below the minimum age a transition rule can specify', whyWrong: 'Transitions at thirty days are entirely ordinary. Object size and object count are what make this plan uneconomic.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.s3.request_charges',
    mode: 'drill',
    nodeIds: ['aws.s3', 'del.tco'],
    difficulty: 'deep',
    explanation:
      'S3 bills for storage and separately for requests, and for workloads made of many small reads the request line dominates. A RAG pipeline fetching one chunk per object per query generates enormous GET volume against trivial storage. Batching chunks into larger objects, or serving them from a database or cache instead, changes the cost shape entirely.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A retrieval service stores every chunk as its own small S3 object and fetches dozens per query. The S3 bill is dominated by requests, not storage. What is the fix?',
      choices: [
        { id: 'a', text: 'Move the chunk objects to a colder and cheaper S3 storage class', whyWrong: 'Storage class changes the storage rate, which is not the line that is large, and colder classes usually raise the per-request charge.' },
        { id: 'b', text: 'Pack chunks into larger objects, or serve them from a database' },
        { id: 'c', text: 'Enable S3 Transfer Acceleration on the bucket holding the chunks', whyWrong: 'That targets long-distance upload throughput and adds a surcharge. It does nothing at all about the number of requests.' },
        { id: 'd', text: 'Put a CloudFront distribution in front of the chunk bucket', whyWrong: 'Caching pays off only when the same chunks are re-read constantly. Retrieval traffic has a long tail, and now you operate a distribution too.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.s3.consistency',
    mode: 'drill',
    nodeIds: ['aws.s3', 'data.idempotency'],
    difficulty: 'intro',
    explanation:
      'S3 provides strong read-after-write consistency for object operations, so a successful PUT is immediately visible to a subsequent GET. When a pipeline still reads stale data the cause is almost always downstream: a cached listing, a metastore partition index that has not been refreshed, or a consumer that captured the object list before the write.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A pipeline writes to S3 and a downstream job sometimes reads the previous version. Where do you investigate?',
      choices: [
        { id: 'a', text: 'S3 eventual consistency, which calls for a retry loop with backoff', whyWrong: 'Object reads are strongly consistent after a successful write. Adding retries hides whatever is genuinely stale instead of fixing it.' },
        { id: 'b', text: 'Cross-region replication lag between source and replica buckets', whyWrong: 'Relevant only if the reader points at a replica, which is worth confirming but is not the general explanation for this symptom.' },
        { id: 'c', text: 'Downstream caches and stale object listings held by the reader' },
        { id: 'd', text: 'Missing versioning on the bucket, so an older version is returned', whyWrong: 'Versioning retains prior versions for recovery. It has no effect on which version a plain GET hands back.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.s3.prefix_throughput',
    mode: 'drill',
    nodeIds: ['aws.s3', 'scale.hotspots'],
    difficulty: 'deep',
    explanation:
      'S3 scales request rate per partitioned prefix, so a naming scheme that funnels a burst into one prefix can produce 503 slow-down responses while the bucket as a whole is nowhere near its capability. Spreading writes across many prefixes, or letting a date-based scheme include a high-cardinality component early in the key, restores headroom.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'An ingest job writing every file under a single date prefix starts getting slow-down errors from S3. What is happening?',
      choices: [
        { id: 'a', text: 'The bucket has reached its maximum permitted total object count', whyWrong: 'Buckets have no fixed object count limit at all. What you are hitting is the per-prefix request rate.' },
        { id: 'b', text: 'The account needs a Service Quotas increase for S3 storage volume', whyWrong: 'There is no storage quota to raise, and a slow-down response is a request-rate signal rather than a capacity one.' },
        { id: 'c', text: 'The objects are large enough to require multipart upload instead', whyWrong: 'Multipart helps with large-object throughput and restartability, not with per-prefix request-rate throttling.' },
        { id: 'd', text: 'Request rate scales per prefix and the whole burst lands on one' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.s3.block_public',
    mode: 'drill',
    nodeIds: ['aws.s3', 'sec.audit'],
    difficulty: 'intro',
    explanation:
      'S3 Block Public Access overrides any bucket policy or ACL that would make objects public, and it can be set at the account level so no individual bucket owner can opt out. Turning it on organization-wide removes an entire category of breach from the customer’s risk register.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What is the simplest account-wide control against an S3 bucket being made public by mistake?',
      choices: [
        { id: 'a', text: 'Enable S3 Block Public Access as an account-level setting' },
        { id: 'b', text: 'An AWS Config rule that reports publicly accessible buckets daily', whyWrong: 'Detective only. It tells you once the exposure already exists, which beats nothing but is not the same as preventing it.' },
        { id: 'c', text: 'Remove s3:PutBucketPolicy from the roles developers use daily', whyWrong: 'It closes one path while leaving ACLs, presigned patterns and every role that legitimately manages bucket policies.' },
        { id: 'd', text: 'Turn on default encryption with a customer managed key everywhere', whyWrong: 'Encryption at rest has nothing to say about whether an anonymous caller is authorized to read the object.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.s3.class_match',
    mode: 'drill',
    nodeIds: ['aws.s3', 'del.tco'],
    difficulty: 'core',
    explanation:
      'Storage class selection is an access-pattern question, not a price-list question. The expensive mistakes are archiving data that turns out to be read weekly, and leaving genuinely cold data on the standard class for years because nobody could predict the pattern.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each S3 access pattern to the storage approach that fits.',
      pairs: [
        { left: 'Read many times a day, latency sensitive', right: 'S3 Standard' },
        { left: 'Unknown or changing access pattern', right: 'S3 Intelligent-Tiering' },
        { left: 'Read a few times a year, needs to be instant', right: 'Glacier Instant Retrieval' },
        { left: 'Compliance copy, retrieval measured in hours', right: 'Glacier Deep Archive' },
      ],
    },
  },

  // ── DynamoDB ─────────────────────────────────────────────────────────────
  {
    id: 'aw.ddb.hot_partition',
    mode: 'drill',
    nodeIds: ['aws.dynamodb', 'scale.hotspots'],
    difficulty: 'deep',
    explanation:
      'DynamoDB spreads a table across partitions by hash of the partition key, and any single partition has a throughput ceiling well below the table total. A key like tenant id where one tenant is ten times everyone else concentrates traffic on one partition, so you see throttling while the table-level metrics look comfortable. Adaptive capacity absorbs some of this, but it cannot exceed the per-partition ceiling.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Writes to a DynamoDB table are being throttled although consumed capacity sits well under the provisioned total. What is the most likely cause?',
      choices: [
        { id: 'a', text: 'Provisioned capacity has not finished scaling up to meet the load', whyWrong: 'A scaling delay produces throttling that clears within minutes and tracks a traffic ramp, not steady throttling at low aggregate utilization.' },
        { id: 'b', text: 'A hot partition: one key value absorbs most of the traffic' },
        { id: 'c', text: 'Items are large enough to consume several write units apiece', whyWrong: 'Large items consume more capacity units, which shows up as high consumed capacity. Here consumption is reported as low.' },
        { id: 'd', text: 'Strongly consistent reads are consuming double the read capacity', whyWrong: 'Read consistency affects read capacity units, and the symptom described is throttling on the write path.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.ddb.write_sharding',
    mode: 'drill',
    nodeIds: ['aws.dynamodb', 'scale.hotspots'],
    difficulty: 'deep',
    explanation:
      'Fixing a hot partition means changing the key so traffic spreads, and the cost is on the read path: a sharded key has to be fanned out across shards at query time. Doing this well means confirming the skew first, choosing a shard count from the observed concentration, and backfilling before cutting reads over.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Sequence the work to remove a hot partition from a DynamoDB table using write sharding.',
      steps: [
        'Confirm the skew from CloudWatch Contributor Insights rather than assuming which key is hot',
        'Choose a shard count from the observed concentration and decide how the suffix is derived',
        'Write new items to the sharded key while continuing to serve reads from the old shape',
        'Backfill existing items into the sharded key space',
        'Switch reads to the fan-out query across shards and remove the old path',
      ],
    },
  },
  {
    id: 'aw.ddb.single_table',
    mode: 'drill',
    nodeIds: ['aws.dynamodb', 'del.handover'],
    difficulty: 'deep',
    explanation:
      'Single-table design packs several entity types into one table so a single query returns a whole object graph, which removes round trips and joins. The price is that the key schema encodes your access patterns, so a new query the design did not anticipate can mean a new index or a migration, and the table is much harder for a new team to read. It is a real technique with a real handover cost.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A team proposes single-table DynamoDB design for a new service. What is the trade-off you make sure they have understood?',
      choices: [
        { id: 'a', text: 'Sharing provisioned capacity across entity types raises the total cost', whyWrong: 'Shared capacity generally smooths utilization and lowers what you provision. Cost is not the objection worth raising here.' },
        { id: 'b', text: 'DynamoDB caps how many distinct entity types one table may hold', whyWrong: 'There is no such cap. Beyond the key attributes the table is schemaless and indifferent to how many shapes you store.' },
        { id: 'c', text: 'The key schema fixes today’s access patterns and reads as opaque' },
        { id: 'd', text: 'Transactional writes cannot span items inside a single-table design', whyWrong: 'Transactions across items in one table are supported, and that capability is one of the arguments in the design’s favor.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.ddb.gsi_throttle',
    mode: 'drill',
    nodeIds: ['aws.dynamodb', 'prod.incident'],
    difficulty: 'edge',
    explanation:
      'A global secondary index has its own throughput, and in provisioned mode a write that cannot be replicated into an under-provisioned index eventually causes throttling on the base table write itself. The failure looks bizarre from the application side, because the writes being rejected have nothing obviously to do with the index. Checking index-level consumed capacity is the shortcut.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Base-table writes on a provisioned DynamoDB table start failing after a new query pattern went live. Base table capacity looks fine. What do you check?',
      choices: [
        { id: 'a', text: 'Whether the new global secondary index is still backfilling itself', whyWrong: 'Backfill consumes capacity and leaves query results incomplete, but a finished index that is simply under-provisioned is the more common cause of ongoing write failures.' },
        { id: 'b', text: 'Whether the new queries are issuing eventually consistent reads', whyWrong: 'Read consistency on an index changes what a query returns and costs. It has no bearing on write throttling at all.' },
        { id: 'c', text: 'The four-hundred-kilobyte item size limit on the base table', whyWrong: 'Oversized items fail deterministically with a validation error on every attempt, not intermittently after a query pattern ships.' },
        { id: 'd', text: 'The index’s own provisioned capacity, which throttles base writes' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.ddb.wrong_store',
    mode: 'drill',
    nodeIds: ['aws.dynamodb', 'aws.rds_aurora'],
    difficulty: 'intro',
    explanation:
      'DynamoDB is excellent when the access patterns are known and stable, and poor when analysts want to ask questions nobody designed for. If the requirement is ad-hoc filtering, joins and aggregations across the data, a relational engine is the correct answer, and saying so is more useful than making the key-value store impersonate one with a dozen indexes.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Business users need to run ad-hoc filtered aggregations across an operational dataset that has no fixed query pattern. Which store do you steer toward?',
      choices: [
        { id: 'a', text: 'A relational engine such as Aurora, where a planner handles the joins' },
        { id: 'b', text: 'DynamoDB with a global secondary index per anticipated query shape', whyWrong: 'Ad-hoc queries cannot be enumerated in advance, and every index you add carries write cost and its own capacity to manage.' },
        { id: 'c', text: 'DynamoDB with Scan plus a filter expression on each question', whyWrong: 'Scan reads the whole table and filters afterwards, burning capacity proportional to table size for every question anyone asks.' },
        { id: 'd', text: 'DynamoDB with a caching layer sitting in front of the table', whyWrong: 'A cache accelerates repeated identical queries. Ad-hoc analysis, where each question is new, is the case it helps least.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.ddb.op_match',
    mode: 'drill',
    nodeIds: ['aws.dynamodb'],
    difficulty: 'intro',
    explanation:
      'Most DynamoDB performance problems trace back to using the wrong operation for the job, above all reaching for Scan when the partition key is known. Getting the operation vocabulary straight is the cheapest correction available.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each DynamoDB operation to the situation it belongs in.',
      pairs: [
        { left: 'GetItem', right: 'You know the full primary key of one item' },
        { left: 'Query', right: 'You know the partition key and want a range of sort keys' },
        { left: 'Scan', right: 'A full-table pass you have accepted the cost of, such as an export' },
        { left: 'BatchWriteItem', right: 'Bulk puts and deletes where per-item conditions are not needed' },
      ],
    },
  },

  // ── RDS & Aurora ─────────────────────────────────────────────────────────
  {
    id: 'aw.aurora.failover_dns',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'prod.incident'],
    difficulty: 'edge',
    explanation:
      'Aurora failover promotes a replica and repoints the cluster writer endpoint in DNS. Clients that cache the resolved address past its TTL, which some JVM configurations do indefinitely, keep opening connections to a host that is now a reader and get write errors long after the cluster is healthy. Fixing the client DNS caching, or putting RDS Proxy in the path, is the durable answer.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'After an Aurora failover the cluster is healthy in the console, but one application keeps throwing read-only errors on writes. Why?',
      choices: [
        { id: 'a', text: 'The application is pointed at the reader endpoint by configuration', whyWrong: 'Worth ruling out, but that would have been failing before the failover as well, rather than starting at the moment of promotion.' },
        { id: 'b', text: 'The client cached the writer address past its TTL and never re-resolved' },
        { id: 'c', text: 'Aurora holds the cluster read-only for a cooldown after failover', whyWrong: 'No such cooldown exists. The promoted instance accepts writes as soon as promotion completes.' },
        { id: 'd', text: 'The connection pool needs resizing for the newly promoted instance', whyWrong: 'Pool size governs concurrency and queueing. It has no influence on whether the target it connects to will accept a write.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.aurora.failover_order',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'scale.multiregion'],
    difficulty: 'core',
    explanation:
      'Aurora separates compute from a shared storage layer replicated across availability zones, which is why failover is a promotion rather than a data copy. Knowing the sequence lets you give a customer a defensible answer about what actually happens to in-flight transactions during an unplanned failover.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Put the events of an unplanned Aurora writer failover in order.',
      steps: [
        'The writer instance becomes unhealthy and in-flight transactions on it are lost',
        'A reader is promoted to writer against the same shared storage volume',
        'The cluster writer endpoint is updated to resolve to the promoted instance',
        'Clients reconnect and resume writes once they re-resolve the endpoint',
        'A replacement reader is provisioned to restore the previous replica count',
      ],
    },
  },
  {
    id: 'aw.aurora.replica_lag',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'client.state'],
    difficulty: 'core',
    explanation:
      'The Aurora reader endpoint load balances across replicas that are a short but nonzero interval behind the writer. A user who submits a form and is immediately redirected to a page served from a reader can see their own change missing. Routing reads that must reflect a just-completed write to the writer, or holding read-your-own-writes traffic on the writer for a short window, is the standard fix.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Users occasionally report that a record they just saved is missing when the next page loads. The app sends reads to the Aurora reader endpoint. What do you change?',
      choices: [
        { id: 'a', text: 'Add more read replicas so the replication lag on each one falls', whyWrong: 'Replicas do not reduce each other’s lag. Adding them spreads read load while the visibility gap after a write stays exactly the same.' },
        { id: 'b', text: 'Enable strongly consistent reads on the Aurora reader endpoint', whyWrong: 'There is no such setting for Aurora replicas. Read consistency here is decided by which endpoint the query goes to.' },
        { id: 'c', text: 'Send reads that must reflect a fresh write to the writer endpoint' },
        { id: 'd', text: 'Wrap the write and the following read inside one transaction', whyWrong: 'They are two separate requests from the browser with a redirect in between. No transaction can span that boundary.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.aurora.serverless_v2',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'scale.autoscaling'],
    difficulty: 'core',
    explanation:
      'Aurora Serverless v2 scales capacity within a configured minimum and maximum, and the minimum is what you are billed for even when the database is idle. Setting the minimum too low makes a burst climb from a cold, small capacity and feel slow; setting it high defeats the point. This is a sizing conversation, not a checkbox.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer picks Aurora Serverless v2 for a bursty internal tool and complains the first queries after a quiet period are slow. What do you look at?',
      choices: [
        { id: 'a', text: 'Whether the cluster fully stopped and is paying a cold start penalty', whyWrong: 'Serverless v2 scales capacity up and down rather than stopping the instance. The floor is a capacity floor, not an off switch.' },
        { id: 'b', text: 'Connection pool exhaustion in the application during the first burst', whyWrong: 'Plausible in general, but pool exhaustion tracks concurrency and would not correlate specifically with periods of low activity.' },
        { id: 'c', text: 'Missing indexes on the tables the internal tool queries most', whyWrong: 'Missing indexes make the same queries slow consistently, not only in the first minutes after a quiet stretch.' },
        { id: 'd', text: 'The configured minimum capacity the cluster has scaled down to' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.aurora.proxy_pooling',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'scale.pooling'],
    difficulty: 'core',
    explanation:
      'Every concurrent Lambda execution environment opens its own database connection, so a spike in invocations becomes a spike in connections and the database refuses new ones. Pooling inside the function does not help because each environment has its own pool. RDS Proxy sits outside the compute and multiplexes many client connections onto a small set of database connections.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A Lambda-based API exhausts Aurora connections during traffic spikes. What is the right structural fix?',
      choices: [
        { id: 'a', text: 'Put RDS Proxy between the functions and the cluster to multiplex' },
        { id: 'b', text: 'Add a connection pool inside the function handler and reuse it', whyWrong: 'Each execution environment keeps its own pool, so a thousand concurrent environments still open a thousand or more connections.' },
        { id: 'c', text: 'Increase the instance size to raise the maximum connection limit', whyWrong: 'That buys headroom proportional to instance size while connections scale with concurrency. You are outrunning the problem, not fixing it.' },
        { id: 'd', text: 'Lower the reserved concurrency ceiling on the Lambda function', whyWrong: 'That protects the database by throttling the API, converting a database capacity problem into user-visible request rejections.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.rds.multi_az_vs_replica',
    mode: 'drill',
    nodeIds: ['aws.rds_aurora', 'scale.multiregion'],
    difficulty: 'intro',
    explanation:
      'A Multi-AZ deployment maintains a standby for availability, and in the classic single-standby form that standby serves no traffic. Read replicas exist to scale reads and are a separate feature with separate cost. Customers regularly buy Multi-AZ expecting read capacity and are surprised by the bill and the unchanged performance.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer enabled Multi-AZ on RDS expecting their reporting queries to get faster. What do you explain?',
      choices: [
        { id: 'a', text: 'Multi-AZ splits reads across both zones once you use the cluster endpoint', whyWrong: 'The standby in a classic Multi-AZ deployment serves no traffic at all. There is nothing being split across zones.' },
        { id: 'b', text: 'Multi-AZ buys failover availability, not extra read capacity' },
        { id: 'c', text: 'They need to switch on read routing in the RDS console settings', whyWrong: 'No such toggle exists. Nothing in the console converts a Multi-AZ standby into a queryable reader.' },
        { id: 'd', text: 'The reporting queries are almost certainly missing their indexes', whyWrong: 'Possibly true and worth checking anyway, but it leaves the customer’s incorrect expectation about Multi-AZ entirely intact.' },
      ],
      correctId: 'b',
    },
  },

  // ── Redshift & Athena ────────────────────────────────────────────────────
  {
    id: 'aw.athena.partitioning',
    mode: 'drill',
    nodeIds: ['aws.redshift', 'del.tco'],
    difficulty: 'core',
    explanation:
      'Athena bills by bytes scanned, so cost is a function of table layout rather than query complexity. Columnar formats let it read only the columns referenced, and partitioning lets it skip whole prefixes when the predicate matches the partition key. Text files with no partitions mean every query reads everything.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Athena queries over a large S3 dataset are expensive and the data is stored as uncompressed JSON with no partitions. What has the biggest effect?',
      choices: [
        { id: 'a', text: 'Rewrite the queries so they select fewer rows and fewer columns', whyWrong: 'On an unpartitioned text layout the filters are applied after the data has been read, so bytes scanned barely moves.' },
        { id: 'b', text: 'Move the dataset to a colder and cheaper S3 storage class', whyWrong: 'Storage class changes storage cost and can add retrieval charges. Athena still bills on bytes scanned either way.' },
        { id: 'c', text: 'Convert to compressed columnar files partitioned on the filter column' },
        { id: 'd', text: 'Raise the per-query data scanned limit on the Athena workgroup', whyWrong: 'Workgroup limits cap runaway spend by killing queries. Raising one makes a query read more data, not less.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.redshift.when',
    mode: 'drill',
    nodeIds: ['aws.redshift'],
    difficulty: 'deep',
    explanation:
      'Query-in-place suits exploratory and intermittent analysis where you do not want to run a warehouse. A warehouse earns its cost when the same shaped queries run constantly with many concurrent users and predictable dashboards, because tuned distribution, sort keys and result caching pay off repeatedly. Choosing by workload rhythm rather than by data volume is the correct instinct.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Which workload profile actually argues for Redshift rather than querying S3 with Athena?',
      choices: [
        { id: 'a', text: 'A dataset in S3 measured in hundreds of terabytes and still growing', whyWrong: 'Volume alone does not decide it. Query-in-place handles very large datasets; how often and how concurrently they are queried is the real question.' },
        { id: 'b', text: 'A handful of analysts running exploratory queries a few times a week', whyWrong: 'That is precisely the profile where paying for a warehouse to sit idle most of the week is hardest to justify.' },
        { id: 'c', text: 'Data arriving as deeply nested semi-structured JSON documents', whyWrong: 'Both engines read semi-structured data. Source format is a modeling concern rather than the factor that picks the engine.' },
        { id: 'd', text: 'Many concurrent users hitting the same dashboards all day' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.redshift.dist_key',
    mode: 'drill',
    nodeIds: ['aws.redshift', 'scale.hotspots'],
    difficulty: 'deep',
    explanation:
      'Redshift distributes rows across slices by the distribution key. A key with few distinct values, or one where one value dominates, puts most rows on one slice, so that slice does all the work and the cluster runs at the speed of its most loaded node. The symptom is a query that does not get faster when you add nodes.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A Redshift query barely improves when the cluster is resized upward. What do you suspect?',
      choices: [
        { id: 'a', text: 'Distribution skew, so most rows land on a handful of slices' },
        { id: 'b', text: 'The workload needs concurrency scaling clusters enabled on it', whyWrong: 'Concurrency scaling helps when many queries queue behind one another, not when a single query is bottlenecked on one slice.' },
        { id: 'c', text: 'Table statistics are stale and the planner is choosing badly', whyWrong: 'Stale statistics do produce poor plans, but that shows up as a bad join order rather than indifference to cluster size.' },
        { id: 'd', text: 'The result set is too large to transfer back to the client', whyWrong: 'Result transfer would be slow at any cluster size, and it would not appear as uneven slice work in the query plan.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.athena.small_files',
    mode: 'drill',
    nodeIds: ['aws.redshift', 'data.ingest_patterns'],
    difficulty: 'deep',
    explanation:
      'A streaming writer that lands one small file per micro-batch produces millions of tiny objects, and every query then pays per-file open overhead that dwarfs the time spent reading data. The fix is a compaction step that rewrites each partition into a few large files, which is unglamorous work that most teams discover only after the query latency becomes intolerable.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A partition holding a few gigabytes across two million objects queries far slower than a partition of the same size in a hundred files. What is going on?',
      choices: [
        { id: 'a', text: 'The many-file partition sits on a colder, slower storage class', whyWrong: 'Worth confirming, but object count alone reproduces this effect exactly when both partitions are on identical storage classes.' },
        { id: 'b', text: 'Per-file overhead dominates: it is listing and opening, not reading' },
        { id: 'c', text: 'The query scans considerably more bytes in the many-file case', whyWrong: 'Bytes scanned is roughly the same in both, which is precisely why the cost report fails to explain the latency difference.' },
        { id: 'd', text: 'S3 is throttling the request rate against that single prefix', whyWrong: 'Possible under heavy concurrency, but the baseline slowdown from per-object overhead shows up even with no throttling at all.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.athena.cost_levers',
    mode: 'drill',
    nodeIds: ['aws.redshift', 'prod.cost_monitoring'],
    difficulty: 'core',
    explanation:
      'Because Athena charges on bytes scanned, everything that reduces bytes read reduces cost proportionally: columnar formats, compression, partition pruning and selecting fewer columns. Workgroup data scanned limits do not reduce cost per query but they stop one bad query from producing an unforgettable invoice.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which measures reduce or contain what a customer spends on Athena? Select all that apply.',
      choices: [
        { id: 'a', text: 'Store data in a compressed columnar format so only referenced columns are read' },
        { id: 'b', text: 'Partition on the column that appears in most WHERE clauses so whole prefixes are skipped' },
        { id: 'c', text: 'Set a per-query data scanned limit on the workgroup as a blast-radius control' },
        { id: 'd', text: 'Add more concurrent query slots', whyWrong: 'Athena is serverless and billed on data scanned. There are no slots to add and concurrency does not change the rate.' },
        { id: 'e', text: 'Move the S3 data into a smaller region', whyWrong: 'Region choice affects unit prices marginally and adds transfer cost. It is not a scanned-bytes lever.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── SQS, SNS, EventBridge, Kinesis ───────────────────────────────────────
  {
    id: 'aw.msg.select_match',
    mode: 'drill',
    nodeIds: ['aws.streaming'],
    difficulty: 'core',
    explanation:
      'These four services get chosen by habit far more often than by requirement, and the wrong pick is expensive to undo once producers and consumers exist. The distinguishing questions are whether a message is consumed once or by many, whether history has to be replayable, and whether routing decisions are made by the sender or by the bus.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each AWS messaging service to the job it is the natural fit for.',
      pairs: [
        { left: 'SQS', right: 'Buffer work so one consumer group processes each message once' },
        { left: 'SNS', right: 'Push the same notification to several subscribers immediately' },
        { left: 'EventBridge', right: 'Route events to targets by content rules, with archive and replay' },
        { left: 'Kinesis Data Streams', right: 'Ordered, replayable stream several consumers read independently' },
      ],
    },
  },
  {
    id: 'aw.msg.fanout_replay',
    mode: 'drill',
    nodeIds: ['aws.streaming', 'data.cdc'],
    difficulty: 'deep',
    explanation:
      'When several independent consumers each need the full ordered history, and a new consumer must be able to start from the beginning of the retention window, you need a stream with retained records rather than a queue that deletes on acknowledgement. That replay property is the reason a stream is chosen, and it is what lets you add an analytics consumer six months later without asking producers to resend anything.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Three teams each need to process every change event in order, and a fourth team will be added later and must reprocess history. Which primitive?',
      choices: [
        { id: 'a', text: 'A single SQS queue that all four consumer groups read from', whyWrong: 'The consumers would compete for messages, so each event is handled once rather than by all four, and deleted messages cannot be replayed.' },
        { id: 'b', text: 'An SNS topic with one subscription per consuming team', whyWrong: 'Fan-out works, but SNS pushes and forgets. A subscriber added later receives nothing that was published before it subscribed.' },
        { id: 'c', text: 'A Kinesis data stream, where each consumer holds its own position' },
        { id: 'd', text: 'Four separate SQS queues the producer writes each event to', whyWrong: 'The producer is now coupled to the consumer list, and there is still no retained history for the fourth team to reprocess.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.msg.fifo',
    mode: 'drill',
    nodeIds: ['aws.streaming', 'data.idempotency'],
    difficulty: 'core',
    explanation:
      'A FIFO queue orders messages within a message group, not across the whole queue, and processing within a group is serialized. Using the customer id as the message group gives you per-customer ordering while different customers still process in parallel, which is almost always the real requirement. Using one constant group id serializes everything and destroys throughput.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Events must be processed strictly in order per customer, but customers are independent. How do you configure an SQS FIFO queue?',
      choices: [
        { id: 'a', text: 'Use one constant message group id for every message on the queue', whyWrong: 'That serializes every customer behind every other customer, collapsing throughput to a single message in flight at a time.' },
        { id: 'b', text: 'Generate a unique message group id for each individual message', whyWrong: 'Every message becomes its own group with no ordering relationship to any other, which is a standard queue at FIFO prices.' },
        { id: 'c', text: 'Use a standard queue and sort by timestamp inside the consumer', whyWrong: 'The consumer cannot tell whether an earlier message is still in flight, so it can never safely conclude it holds the next one.' },
        { id: 'd', text: 'Use the customer id as the message group id on every message' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.msg.eventbridge_routing',
    mode: 'drill',
    nodeIds: ['aws.streaming', 'prod.progressive'],
    difficulty: 'core',
    explanation:
      'EventBridge decouples producers from the routing decision: publishers emit an event, and rules matched on event content decide which targets receive it. Adding a consumer is a rule, not a change to the publisher. Archive and replay on the bus also let you re-drive events into a target you added after the fact.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A platform team wants new consumers to be able to subscribe to existing domain events without any change to the publishing services. What supports that best?',
      choices: [
        { id: 'a', text: 'An EventBridge bus where a rule match routes events to new targets' },
        { id: 'b', text: 'An SNS topic per event type, with subscriptions added as needed', whyWrong: 'Workable, but the publisher owns the topic list, so every new routing distinction means a new topic and a change to the publisher.' },
        { id: 'c', text: 'A shared SQS queue that each consumer filters after receiving', whyWrong: 'Only one consumer receives each message, and filtering after delivery means every consumer handles traffic it has no business seeing.' },
        { id: 'd', text: 'Direct HTTP calls from the publisher to each registered consumer', whyWrong: 'This is exactly the coupling the requirement was written to avoid, and it makes the publisher responsible for consumer availability.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.msg.dlq_order',
    mode: 'drill',
    nodeIds: ['aws.streaming', 'prod.oncall'],
    difficulty: 'core',
    explanation:
      'A poison message that fails forever will otherwise be retried forever, blocking or looping the consumer. A redrive policy moves it aside after a set number of receives so the queue keeps draining, and the dead-letter queue then needs an alarm and a documented redrive path, otherwise it becomes a silent graveyard nobody reads.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Order how a poison message should be handled on an SQS queue with a dead-letter queue configured.',
      steps: [
        'The consumer fails the message and it becomes visible again after the visibility timeout',
        'Retries continue until the receive count reaches the redrive policy threshold',
        'SQS moves the message to the dead-letter queue and the main queue keeps draining',
        'An alarm on dead-letter queue depth pages the owning team',
        'The team fixes the cause and redrives the message back to the source queue',
      ],
    },
  },
  {
    id: 'aw.msg.sns_vs_sqs',
    mode: 'drill',
    nodeIds: ['aws.streaming'],
    difficulty: 'intro',
    explanation:
      'SNS pushes each published message to all current subscribers; SQS holds messages until a consumer pulls and deletes them, and each message goes to one consumer. Pairing them, with SNS fanning out into per-consumer SQS queues, is the standard pattern when you need both broadcast and durable buffering.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What is the core behavioral difference between SNS and SQS?',
      choices: [
        { id: 'a', text: 'SNS guarantees ordering across subscribers and SQS does not', whyWrong: 'Standard SNS and standard SQS both make no ordering guarantee, and both offer FIFO variants. Ordering is not what separates them.' },
        { id: 'b', text: 'SNS pushes to every subscriber; SQS is pulled and consumed once' },
        { id: 'c', text: 'SQS delivers each message to many subscribers and SNS does not', whyWrong: 'Reversed. Fan-out to many subscribers is SNS’s defining behavior, while an SQS message is deleted by whichever consumer takes it.' },
        { id: 'd', text: 'SNS stores messages durably until a subscriber acknowledges them', whyWrong: 'SNS delivers with retries but is not a durable buffer. Putting an SQS queue behind the topic is what supplies that property.' },
      ],
      correctId: 'b',
    },
  },

  // ── KMS ──────────────────────────────────────────────────────────────────
  {
    id: 'aw.kms.key_policy',
    mode: 'drill',
    nodeIds: ['aws.kms', 'aws.iam'],
    difficulty: 'deep',
    explanation:
      'A KMS key has its own resource policy, and unlike most AWS resources it is not optional: if the key policy does not delegate to IAM, then no IAM policy in the account grants access to it. This is deliberate, because it lets a security team hold a key that even an account administrator cannot use, and it is the source of a great many baffling access-denied errors.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'An account administrator with a full-access IAM policy gets access denied when decrypting with a KMS key. Explain it.',
      choices: [
        { id: 'a', text: 'The key lives in a different region from the caller’s endpoint', whyWrong: 'A cross-region key reference fails to resolve as a key at all. It does not come back as an authorization denial on a valid key.' },
        { id: 'b', text: 'The key has already been scheduled for deletion by someone else', whyWrong: 'A key pending deletion returns a specific key-state error naming that state, not a plain access denial.' },
        { id: 'c', text: 'The key policy neither grants them nor delegates to IAM at all' },
        { id: 'd', text: 'The administrator has not been added to a grant on that key', whyWrong: 'Grants are one valid way to delegate, but the reason the IAM policy is being ignored is the key policy, and that is where the fix belongs.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.kms.grants',
    mode: 'drill',
    nodeIds: ['aws.kms', 'sec.zero_trust'],
    difficulty: 'core',
    explanation:
      'Grants are a way to delegate specific KMS operations to a principal temporarily and programmatically, and they can be retired when the work is done. AWS services use them internally for exactly this reason: a long-lived key policy entry for a short-lived need is the thing you are trying to avoid, and grants are auditable and revocable independently.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A batch job needs to decrypt with a KMS key for the duration of a migration and then never again. What mechanism fits the requirement most precisely?',
      choices: [
        { id: 'a', text: 'Add the job’s role to the key policy and edit it out afterwards', whyWrong: 'It works, but it depends on somebody remembering the cleanup, and a key policy edit is a heavier and riskier change than a grant.' },
        { id: 'b', text: 'Create a second KMS key dedicated to the migration workload', whyWrong: 'The data is already encrypted under the existing key. A new key grants no access to any of the ciphertext that exists.' },
        { id: 'c', text: 'Give the job an IAM policy allowing kms:Decrypt on that key ARN', whyWrong: 'Necessary but not sufficient unless the key policy delegates to IAM, and it leaves a standing permission behind afterwards.' },
        { id: 'd', text: 'A KMS grant scoped to decrypt, retired when the work finishes' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.kms.retire_order',
    mode: 'drill',
    nodeIds: ['aws.kms', 'prod.rollback'],
    difficulty: 'core',
    explanation:
      'Deleting a KMS key is irreversible and makes everything encrypted under it permanently unreadable, which is why scheduled deletion enforces a waiting period. The safe retirement sequence disables the key first, because disabling is instant and reversible and will surface any forgotten dependency as a clean error while you can still undo it.',
    citations: cite('awsIam'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Order the steps to retire a KMS key that you believe is no longer in use.',
      steps: [
        'Check CloudTrail for recent cryptographic operations naming that key',
        'Re-encrypt any data you find still under it with the replacement key',
        'Disable the key, which is immediate and reversible, and watch for errors',
        'Leave it disabled long enough to cover infrequent jobs such as monthly batches',
        'Schedule deletion and let the mandatory waiting period run',
      ],
    },
  },
  {
    id: 'aw.kms.cmek_proves',
    mode: 'drill',
    nodeIds: ['aws.kms', 'sec.soc2'],
    difficulty: 'edge',
    explanation:
      'A customer managed key gives the customer the ability to disable or delete the key, an audit trail of every cryptographic operation, and control over the key policy. What it does not give is protection from a service that is legitimately authorized to use the key on their behalf, or any guarantee about where data is processed. Being precise about this in a security review is the difference between a durable answer and one that unravels.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A prospect asks what a customer managed KMS key actually gives them beyond default encryption. What is the accurate answer?',
      choices: [
        { id: 'a', text: 'Key policy control, a CloudTrail record of use, and a disable switch' },
        { id: 'b', text: 'An assurance that no provider employee can ever see the plaintext', whyWrong: 'Overclaiming. While the key is enabled and the service is authorized, that service still decrypts data in order to operate on it.' },
        { id: 'c', text: 'An assurance that the encrypted data never leaves a chosen region', whyWrong: 'Residency comes from region and service configuration. Owning the key places no constraint on where processing happens.' },
        { id: 'd', text: 'A stronger encryption algorithm than the default service key uses', whyWrong: 'The algorithm is identical. What changes is who holds the key policy and who can revoke access to the ciphertext.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.kms.multiregion',
    mode: 'drill',
    nodeIds: ['aws.kms', 'scale.multiregion'],
    difficulty: 'core',
    explanation:
      'A standard KMS key is regional, so ciphertext produced in one region cannot be decrypted in another. Multi-Region keys share key material across replicas so data encrypted in one region can be decrypted by the replica in another, which is what makes cross-region replication of encrypted data or a warm standby workable without a re-encryption step.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A disaster recovery plan replicates encrypted S3 data to a second region. What has to be true about the KMS key?',
      choices: [
        { id: 'a', text: 'Nothing, since KMS keys are global and usable from any region', whyWrong: 'Standard KMS keys are regional. Assuming otherwise produces a recovery plan that fails on the one day you actually need it.' },
        { id: 'b', text: 'The destination needs a Multi-Region replica of the source key' },
        { id: 'c', text: 'The source key has to be shared with the destination account', whyWrong: 'The constraint here is regional rather than cross-account, and sharing a regional key does not make it usable elsewhere.' },
        { id: 'd', text: 'Data must be decrypted before replication and re-encrypted after', whyWrong: 'Possible, but it puts plaintext through your pipeline, which is exactly the design a Multi-Region key exists to avoid.' },
      ],
      correctId: 'b',
    },
  },

  // ── Observability ────────────────────────────────────────────────────────
  {
    id: 'aw.cw.cardinality',
    mode: 'drill',
    nodeIds: ['aws.observability', 'prod.cost_monitoring'],
    difficulty: 'deep',
    explanation:
      'CloudWatch treats every distinct combination of dimension values as its own custom metric, and custom metrics are billed per metric. Adding a user id or request id as a dimension therefore creates one metric per user, which turns a monitoring change into a bill that outgrows the workload it is monitoring. High-cardinality attributes belong in logs, not in metric dimensions.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A team adds a user id dimension to a CloudWatch custom metric so they can chart per-user latency. What goes wrong?',
      choices: [
        { id: 'a', text: 'CloudWatch rejects any dimension carrying more than a fixed value count', whyWrong: 'No such rejection exists. The metrics are accepted quietly, which is precisely why the invoice is what tells you about it.' },
        { id: 'b', text: 'Per-user values are aggregated automatically and the dimension dropped', whyWrong: 'Dimensions are never silently dropped. Each distinct combination is stored and charged as its own custom metric.' },
        { id: 'c', text: 'Each dimension combination is a billed metric, so cost tracks users' },
        { id: 'd', text: 'Metric resolution degrades to five minutes once cardinality is high', whyWrong: 'Resolution is a property you choose when publishing a metric. It does not degrade as cardinality grows.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.cw.retention',
    mode: 'drill',
    nodeIds: ['aws.observability', 'prod.cost_monitoring'],
    difficulty: 'intro',
    explanation:
      'A new CloudWatch log group retains data indefinitely unless a retention period is set, so a chatty debug logger quietly accumulates storage forever. Setting retention at log group creation, ideally enforced as a Config rule or in the module every team uses, is one of the highest-value small governance wins on an AWS account.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'CloudWatch Logs storage has become a major line item on a two-year-old account. What is the most likely reason?',
      choices: [
        { id: 'a', text: 'Logs Insights queries are being billed as stored log data', whyWrong: 'Query charges are billed separately on data scanned by the query. They never roll up into the storage line.' },
        { id: 'b', text: 'CloudWatch replicates log groups across regions by default', whyWrong: 'There is no default cross-region replication for log groups. Nothing is being duplicated on your behalf.' },
        { id: 'c', text: 'Metric filters keep a second copy of every matching log event', whyWrong: 'A metric filter emits a metric datapoint from matching events. It stores no second copy of the log data.' },
        { id: 'd', text: 'The log groups have no retention set, so nothing has ever expired' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.cw.emf',
    mode: 'drill',
    nodeIds: ['aws.observability', 'ai.observability'],
    difficulty: 'deep',
    explanation:
      'Embedded Metric Format lets a workload write a structured log line that CloudWatch parses into metrics, keeping only the low-cardinality fields as dimensions while the high-cardinality context stays queryable in the log. That gives you dashboards on the aggregate and the ability to find the individual slow request, without paying per-metric for every tenant.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'You need aggregate latency dashboards plus the ability to investigate a single tenant’s slow requests, without a metric per tenant. What do you reach for?',
      choices: [
        { id: 'a', text: 'Embedded Metric Format, promoting only low-cardinality fields' },
        { id: 'b', text: 'One high-resolution custom metric published per tenant identifier', whyWrong: 'That is precisely the per-tenant metric explosion the requirement rules out, and high resolution makes each one cost more.' },
        { id: 'c', text: 'X-Ray traces captured on every request at full sampling', whyWrong: 'Traces are excellent for one specific slow request and a poor, expensive substitute for aggregate latency dashboards.' },
        { id: 'd', text: 'A CloudWatch metric filter matching the tenant id in each log', whyWrong: 'A filter that emits one metric per tenant id lands you straight back in the cardinality problem you were avoiding.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.obs.signal_match',
    mode: 'drill',
    nodeIds: ['aws.observability'],
    difficulty: 'intro',
    explanation:
      'Observability questions have shapes, and each shape has a tool that answers it cheaply. Teams that reach for logs to answer every question end up scanning terabytes to compute something a metric would have given them instantly, and teams with only metrics cannot explain why a single request was slow.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each question to the AWS telemetry surface that answers it most directly.',
      pairs: [
        { left: 'Is error rate above budget right now?', right: 'CloudWatch metric with an alarm' },
        { left: 'Where did this one slow request spend its time?', right: 'An X-Ray trace' },
        { left: 'What exactly did this failing job print?', right: 'CloudWatch Logs' },
        { left: 'Which API caller deleted that resource?', right: 'CloudTrail' },
      ],
    },
  },
  {
    id: 'aw.cw.alarm_quality',
    mode: 'drill',
    nodeIds: ['aws.observability', 'prod.oncall'],
    difficulty: 'deep',
    explanation:
      'An alarm earns its place by being actionable, tied to something a user would notice, and accompanied by a runbook. Alarms on raw resource utilization tend to fire during healthy load and get muted, and once a team learns to ignore an alarm channel, the one that mattered is ignored with it.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which properties make a CloudWatch alarm worth paging a human for? Select all that apply.',
      choices: [
        { id: 'a', text: 'It measures something a user would experience, such as error rate or latency against an objective' },
        { id: 'b', text: 'There is a documented action the person woken up can actually take' },
        { id: 'c', text: 'Its threshold and evaluation period are set so it does not fire during normal load patterns' },
        { id: 'd', text: 'It fires on CPU utilization crossing a fixed percentage', whyWrong: 'High CPU is often a healthy system doing work. As a paging signal it produces noise that trains people to ignore the channel.' },
        { id: 'e', text: 'It covers every metric the service emits, so nothing is missed', whyWrong: 'Alarming on everything guarantees alert fatigue. Coverage is a dashboard goal, not a paging goal.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── Billing & commitments ────────────────────────────────────────────────
  {
    id: 'aw.bill.sp_vs_ri',
    mode: 'drill',
    nodeIds: ['aws.billing', 'del.tco'],
    difficulty: 'deep',
    explanation:
      'Compute Savings Plans commit to an hourly spend and apply flexibly across instance families, sizes, regions and operating systems, and across Fargate and Lambda too. EC2 Instance Savings Plans commit to a family in a region for a deeper discount. Reserved Instances are tied to specific attributes and are the right answer mainly when you also want a zonal capacity reservation.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer expects steady compute spend but keeps changing instance families as they tune workloads. Which commitment fits?',
      choices: [
        { id: 'a', text: 'Standard Reserved Instances bought for the family they run today', whyWrong: 'Standard RIs lock to the attributes this customer keeps changing, so the discount stops applying to whatever they actually run next.' },
        { id: 'b', text: 'A Compute Savings Plan on their expected hourly spend' },
        { id: 'c', text: 'An EC2 Instance Savings Plan on the current family and region', whyWrong: 'A deeper discount, but it commits to one family in one region, which is exactly the flexibility they said they need to keep.' },
        { id: 'd', text: 'On-demand pricing paired with a monthly budget alert threshold', whyWrong: 'A budget alert manages surprise rather than cost. Steady, predictable spend is the case where a commitment pays for itself.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.bill.capacity',
    mode: 'drill',
    nodeIds: ['aws.billing', 'scale.capacity'],
    difficulty: 'edge',
    explanation:
      'Savings Plans are purely a billing construct: they reduce the rate you pay and reserve nothing. If a customer needs assurance that capacity will be available in a specific availability zone at a specific time, that is an On-Demand Capacity Reservation, which is a separate purchase and can itself be covered by a Savings Plan. Conflating the two produces a launch-day failure with a discounted invoice.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer bought Savings Plans and assumes capacity is now guaranteed for their scale-out event. Correct them.',
      choices: [
        { id: 'a', text: 'Savings Plans reserve capacity only for the committed instance family', whyWrong: 'They reserve no capacity at all, for any family. There is no partial guarantee hiding in the commitment to lean on.' },
        { id: 'b', text: 'Capacity is assured once the plan covers more than their peak spend', whyWrong: 'Coverage level describes how much of the bill gets the discounted rate. It has no relationship to instance availability.' },
        { id: 'c', text: 'The plan discounts the rate; capacity needs a reservation' },
        { id: 'd', text: 'Converting the plan to Reserved Instances would supply the guarantee', whyWrong: 'Only zonal RIs carry a capacity reservation and regional ones do not, so the advice is wrong as stated and points away from the right instrument.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.bill.tags',
    mode: 'drill',
    nodeIds: ['aws.billing', 'prod.cost_monitoring'],
    difficulty: 'core',
    explanation:
      'A tag only appears as a cost dimension after it is activated as a cost allocation tag in the management account, and activation is not retroactive: past billing data is not re-tagged. So the first month of a chargeback program is spent producing nothing useful unless someone turned the tags on before the workloads launched.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer tagged every resource with a cost center but Cost Explorer shows nothing broken down that way. What did they miss?',
      choices: [
        { id: 'a', text: 'Cost Explorer groups only by AWS-generated tags, not user-defined ones', whyWrong: 'User-defined tags are fully supported once they have been activated. The activation step is the gap, not the kind of tag.' },
        { id: 'b', text: 'Tags have to be applied at resource creation to reach the billing data', whyWrong: 'Tags added later do appear for usage from that point onward. The blocker is activation, not when the tag was attached.' },
        { id: 'c', text: 'They need to enable Cost and Usage Reports before grouping by tag', whyWrong: 'A detailed usage report is useful for other reasons, but Cost Explorer groups by an activated tag without one.' },
        { id: 'd', text: 'The key was never activated as a cost allocation tag' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.bill.quota_order',
    mode: 'drill',
    nodeIds: ['aws.billing', 'scale.capacity'],
    difficulty: 'core',
    explanation:
      'Service quotas are per account and per region, many are adjustable, and adjustments go through a request that takes time. Discovering a quota ceiling during the load test the customer is watching is avoidable with an hour of arithmetic beforehand, and the sequence below is the hour.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Sequence the preparation that stops a service quota from ending a load test early.',
      steps: [
        'Derive the peak concurrent resource counts the test will demand, per service and region',
        'Compare each against the current quota in Service Quotas for that account and region',
        'Raise increase requests for the gaps, allowing lead time for the ones that need review',
        'Confirm the applied values in the target account before the test window opens',
        'Alarm on the quota utilization metrics during the run so a ceiling is visible immediately',
      ],
    },
  },
  {
    id: 'aw.bill.cost_levers',
    mode: 'drill',
    nodeIds: ['aws.billing', 'prod.cost_monitoring'],
    difficulty: 'deep',
    explanation:
      'The AWS bill for a typical workload is rarely dominated by the compute people budgeted for. Idle provisioned capacity, data transfer between availability zones and out to the internet, NAT processing, and log ingestion are the four lines that repeatedly surprise teams, and all four are architectural choices rather than negotiable rates.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Reviewing a surprising AWS bill, which lines most often turn out to be the ones nobody modeled? Select all that apply.',
      choices: [
        { id: 'a', text: 'Cross-availability-zone data transfer between chatty tiers' },
        { id: 'b', text: 'NAT gateway data processing on traffic that could have used endpoints' },
        { id: 'c', text: 'CloudWatch Logs ingestion from debug-level logging left on' },
        { id: 'd', text: 'S3 storage for the application’s own object data', whyWrong: 'Storage is usually the line teams do model, and it is comparatively cheap per gigabyte. The requests and transfer around it are the surprises.' },
        { id: 'e', text: 'IAM role and policy usage', whyWrong: 'IAM itself carries no charge. It never appears as a cost line.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },

  // ── Bedrock ──────────────────────────────────────────────────────────────
  {
    id: 'aw.br.provisioned',
    mode: 'drill',
    nodeIds: ['aws.bedrock', 'scale.capacity'],
    difficulty: 'deep',
    explanation:
      'On-demand model invocation shares capacity and is subject to throttling, which is fine for variable traffic and unacceptable for a launch with a committed latency target. Provisioned throughput buys dedicated model units for a committed term, giving predictable throughput at a cost you pay whether or not you use it. The decision is a forecasting question, not a technical one.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A Bedrock-backed assistant launches to fifty thousand employees on one day and the team is worried about throttling. What do you propose?',
      choices: [
        { id: 'a', text: 'Buy provisioned throughput sized from the forecast launch peak' },
        { id: 'b', text: 'Add exponential backoff with jitter and retries in the client SDK', whyWrong: 'Necessary hygiene either way, but under sustained throttling retries only lengthen every response and make the launch feel broken.' },
        { id: 'c', text: 'Spread the rollout across regions and call whichever answers first', whyWrong: 'It complicates the residency answer and multiplies the surface you must reason about, to route around a capacity problem that has a direct fix.' },
        { id: 'd', text: 'Cache model responses aggressively in front of the assistant', whyWrong: 'Helps for repeated identical prompts, which is a small share of assistant traffic, and it supplies no throughput guarantee at all.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.br.feature_match',
    mode: 'drill',
    nodeIds: ['aws.bedrock'],
    difficulty: 'intro',
    explanation:
      'Bedrock is a set of separable capabilities rather than one product, and customers frequently ask for one when they mean another. Being able to name which piece solves which problem shortens a scoping conversation considerably.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each Bedrock capability to the problem it addresses.',
      pairs: [
        { left: 'Provisioned throughput', right: 'Predictable capacity for a committed term' },
        { left: 'Guardrails', right: 'Enforced content and topic policy outside the model' },
        { left: 'Knowledge Bases', right: 'Managed ingestion, embedding and retrieval for RAG' },
        { left: 'Agents', right: 'Managed orchestration of tool calls across turns' },
        { left: 'Batch inference', right: 'Large offline scoring jobs with no latency requirement' },
      ],
    },
  },
  {
    id: 'aw.br.iam_control',
    mode: 'drill',
    nodeIds: ['aws.bedrock', 'aws.iam'],
    difficulty: 'core',
    explanation:
      'Model access in Bedrock is IAM-controlled at the resource level, so a policy can allow bedrock:InvokeModel on specific model ARNs and nothing else. That is how you let a team experiment with approved models while keeping unapproved ones out of production, and it is far more durable than a written policy nobody enforces.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Governance wants only two approved foundation models usable in the production account. How is that enforced?',
      choices: [
        { id: 'a', text: 'Request model access only for the approved models in the console', whyWrong: 'Access enablement is a useful first gate, but it is an account setting any administrator can change. It is not an enforced boundary.' },
        { id: 'b', text: 'An SCP plus IAM allowing InvokeModel on the approved ARNs only' },
        { id: 'c', text: 'A code review rule that the model id must match the approved list', whyWrong: 'Process is not enforcement. Any deployment that skips review, an incident hotfix above all, walks straight past it.' },
        { id: 'd', text: 'A guardrail configured to reject calls to unapproved models', whyWrong: 'Guardrails evaluate the content of prompts and responses. They have no say in which model may be invoked in the first place.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.br.logging',
    mode: 'drill',
    nodeIds: ['aws.bedrock', 'sec.audit'],
    difficulty: 'core',
    explanation:
      'CloudTrail records that an invocation happened and by whom, but the prompt and completion contents are only captured if model invocation logging is explicitly enabled and pointed at S3 or CloudWatch Logs. Auditors asking "show me what the model was asked" need that turned on in advance, and the moment you turn it on you have created a store of potentially sensitive text that needs its own controls.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'A compliance team wants to reconstruct what was asked of a model and what came back. What has to be in place? Select all that apply.',
      choices: [
        { id: 'a', text: 'Model invocation logging enabled and configured with a destination for prompt and response data' },
        { id: 'b', text: 'Access controls and retention on that destination, since it now holds sensitive prompt text' },
        { id: 'c', text: 'A correlation identifier carried from the application request into the logged invocation' },
        { id: 'd', text: 'CloudTrail alone, which records the full request and response body', whyWrong: 'CloudTrail captures the management and API event, not the prompt and completion payloads. Relying on it leaves the auditor unanswered.' },
        { id: 'e', text: 'A guardrail configured on every invocation', whyWrong: 'Guardrails enforce policy and produce their own trace of interventions, but they are not the record of what was asked and answered.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.br.converse_api',
    mode: 'drill',
    nodeIds: ['aws.bedrock', 'ai.tool_calling'],
    difficulty: 'core',
    explanation:
      'The Converse API gives one message and tool-use shape across the models Bedrock serves, so switching models is a configuration change rather than a rewrite of request construction and response parsing. Building against each provider’s native payload format instead is how a codebase acquires a per-model adapter layer nobody wanted to own.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A team expects to re-evaluate their model choice every few months. What should shape how they write the Bedrock integration?',
      choices: [
        { id: 'a', text: 'Write against each model’s native request format for maximum control', whyWrong: 'It exposes provider-specific fields at the cost of a bespoke adapter per model, which is exactly the friction they want to avoid.' },
        { id: 'b', text: 'Build an internal abstraction layer over the raw invoke calls', whyWrong: 'You would be rebuilding, and then maintaining forever, an abstraction the platform already provides and keeps current for you.' },
        { id: 'c', text: 'Use the Converse API so a model swap is mostly configuration' },
        { id: 'd', text: 'Pin a single model and revisit only if answer quality regresses', whyWrong: 'That is a policy they have already rejected by planning to re-evaluate, and it says nothing about how to write the integration.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.br.batch',
    mode: 'drill',
    nodeIds: ['aws.bedrock', 'ai.cost'],
    difficulty: 'deep',
    explanation:
      'A backfill of millions of documents has no user waiting, which means it should not compete with interactive traffic for the same throughput. Batch inference submits the work as a job reading from and writing to S3, so the latency-insensitive workload stops driving your provisioned capacity sizing and stops throttling the assistant people are actually using.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Classifying an eight-million-document archive with a foundation model is starving the live assistant of throughput. What changes?',
      choices: [
        { id: 'a', text: 'Throttle the backfill in the application and run it overnight', whyWrong: 'Better than nothing, but you are hand-building a scheduler and still buying interactive-path capacity for work with no latency requirement.' },
        { id: 'b', text: 'Buy additional provisioned throughput sized to cover both workloads', whyWrong: 'You would be provisioning peak capacity for a job that does not care when it finishes, which is the most expensive way to fix this.' },
        { id: 'c', text: 'Switch the backfill to a smaller and cheaper foundation model', whyWrong: 'Reasonable for cost and worth testing for quality, but the two workloads still share the same throughput and the contention remains.' },
        { id: 'd', text: 'Run the archive as a Bedrock batch inference job over S3' },
      ],
      correctId: 'd',
    },
  },

  // ── Bedrock Agents ───────────────────────────────────────────────────────
  {
    id: 'aw.bragent.action_group',
    mode: 'drill',
    nodeIds: ['aws.bedrock_agents', 'ai.tool_calling'],
    difficulty: 'core',
    explanation:
      'An action group is how a Bedrock agent is told what it can do: a schema describing the operations and their parameters, and a backing implementation such as a Lambda function. The schema descriptions are prompt text as far as the model is concerned, so vague operation names and thin parameter descriptions produce wrong tool selection far more often than model quality does.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A Bedrock agent keeps calling the wrong action for a common user request. Where do you look first?',
      choices: [
        { id: 'a', text: 'The action group schema, since names and descriptions drive selection' },
        { id: 'b', text: 'The temperature setting on the agent’s orchestration model', whyWrong: 'Temperature adds variance around a choice the model is already making badly. It cannot teach the model which action is which.' },
        { id: 'c', text: 'The execution role on the Lambda function behind the action', whyWrong: 'Permissions decide whether a call succeeds once it has been made. They play no part in which action gets chosen.' },
        { id: 'd', text: 'The idle session timeout configured on the agent alias', whyWrong: 'Session length governs how much conversation history is retained, not the mapping between a user intent and an action.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.bragent.identity',
    mode: 'drill',
    nodeIds: ['aws.bedrock_agents', 'idp.agent_identity'],
    difficulty: 'deep',
    explanation:
      'A managed agent invokes its action groups with a service role, so from the backing function’s perspective every user looks identical. If one user may see a record and another may not, that check has to happen inside the action implementation using an end-user identity you pass through, not in the prompt. Telling the model not to reveal other users’ data is not an access control.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A Bedrock agent queries an internal system where users have different entitlements. How do you keep one user from seeing another’s records?',
      choices: [
        { id: 'a', text: 'Instruct the agent in its prompt to return only the caller’s own data', whyWrong: 'A prompt instruction is a suggestion the model may not follow and an attacker will work to bypass. Authorization cannot live in the prompt.' },
        { id: 'b', text: 'Pass the end-user identity through and check it in the function' },
        { id: 'c', text: 'Create a separate agent and alias for each entitlement level', whyWrong: 'Unmanageable past a handful of levels, and it still does not scope which records the backing function returns on any given call.' },
        { id: 'd', text: 'Give the agent execution role read-only permissions on the system', whyWrong: 'Read-only limits the damage but says nothing about whose records are readable. That role is identical for every user.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.bragent.when_custom',
    mode: 'drill',
    nodeIds: ['aws.bedrock_agents', 'ai.agents'],
    difficulty: 'deep',
    explanation:
      'A managed agent runtime is the right default because it removes orchestration, session handling and retry plumbing you would otherwise own. It becomes the wrong choice when the orchestration itself is the product: bespoke planning, deterministic branching you must be able to unit-test, human approval steps inside the loop, or portability commitments across environments.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which signals suggest moving off a managed Bedrock agent onto your own orchestration? Select all that apply.',
      choices: [
        { id: 'a', text: 'The control flow needs deterministic branching and human approval gates you must be able to test in isolation' },
        { id: 'b', text: 'The planning strategy itself is the differentiator you are building' },
        { id: 'c', text: 'The same agent has to run unchanged in an environment outside AWS' },
        { id: 'd', text: 'The agent needs to call more than a handful of tools', whyWrong: 'Tool count is well within what a managed runtime handles. Poor tool descriptions cause more trouble than tool quantity.' },
        { id: 'e', text: 'The team wants to see the reasoning steps for debugging', whyWrong: 'The managed runtime exposes a trace of orchestration and tool calls. Wanting visibility is not a reason to rebuild it.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.bragent.trace',
    mode: 'drill',
    nodeIds: ['aws.bedrock_agents', 'ai.observability'],
    difficulty: 'core',
    explanation:
      'When a managed agent gives a wrong answer, the useful artifact is the trace: the orchestration steps, which action groups were considered and invoked, what they returned, and how the model used it. Debugging from the final response alone means guessing at which of four stages went wrong, and the trace turns that into reading.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A user reports that an agent gave a confidently wrong answer yesterday. What do you need to diagnose it properly?',
      choices: [
        { id: 'a', text: 'The final response text alongside the question the user typed', whyWrong: 'That confirms it was wrong without showing where it went wrong. Retrieval, tool selection, tool output and generation fail differently.' },
        { id: 'b', text: 'CloudWatch invocation metrics for the agent over that period', whyWrong: 'Volume, latency and error-rate metrics describe the population of calls. They cannot describe one reasoning path.' },
        { id: 'c', text: 'The invocation trace for that session, with each action and result' },
        { id: 'd', text: 'A fresh reproduction of the same question asked again today', whyWrong: 'Worth attempting, but non-determinism and changed data mean a clean reproduction is not guaranteed, and failing to reproduce proves nothing.' },
      ],
      correctId: 'c',
    },
  },

  // ── Knowledge Bases ──────────────────────────────────────────────────────
  {
    id: 'aw.kb.managed_scope',
    mode: 'drill',
    nodeIds: ['aws.knowledge_bases', 'ai.chunking'],
    difficulty: 'core',
    explanation:
      'A Bedrock knowledge base owns the ingestion pipeline: reading the source, chunking it, embedding the chunks, writing them to a vector store, and serving retrieval with citations. What it does not own is deciding what belongs in the corpus, who is allowed to see each document, or whether the chunking strategy suits your document shapes.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer asks what a Bedrock knowledge base takes off their plate versus what stays theirs. What is the accurate split?',
      choices: [
        { id: 'a', text: 'It owns the whole RAG problem, document-level access control included', whyWrong: 'Per-user entitlement is not solved for you anywhere in the managed path. Presenting it as solved is how a data-leak finding gets written.' },
        { id: 'b', text: 'It is a vector database with a friendlier console on top of it', whyWrong: 'It also runs ingestion, chunking, embedding and the retrieval path with citations, which is most of the plumbing you would otherwise build.' },
        { id: 'c', text: 'It removes the need for evaluation, since retrieval is managed', whyWrong: 'Managed retrieval still misses documents and still grounds answers badly. Evaluation matters more when you have fewer knobs to turn.' },
        { id: 'd', text: 'It owns ingestion and retrieval; curation and permissions stay yours' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.kb.sync_order',
    mode: 'drill',
    nodeIds: ['aws.knowledge_bases', 'ai.rag_failure'],
    difficulty: 'core',
    explanation:
      'A knowledge base answers from its index, not from the source, so a document changed in S3 is invisible until an ingestion job runs. Stale-answer complaints are usually this, and diagnosing them means checking freshness before touching prompts or models. Deletions matter as much as edits, because a removed source document keeps being cited until the sync processes the deletion.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'A user says the assistant quoted a policy that was withdrawn last week. Order the diagnosis.',
      steps: [
        'Confirm the source document was actually removed or superseded in the data source',
        'Check when the knowledge base last ran an ingestion job against that data source',
        'Retrieve for the user’s query directly and see whether the stale chunk still comes back',
        'Run a sync so the deletion or update is reflected in the index',
        'Add a scheduled or event-driven sync so freshness is not a manual step',
      ],
    },
  },
  {
    id: 'aw.kb.permissions',
    mode: 'drill',
    nodeIds: ['aws.knowledge_bases', 'ai.rag_failure'],
    difficulty: 'edge',
    explanation:
      'A knowledge base retrieves from an index without any notion of who is asking, so an HR corpus containing documents with different audiences will happily surface a restricted paragraph to anyone. The workable pattern is to carry entitlement attributes as chunk metadata and apply a retrieval filter derived from the caller’s identity, and to keep genuinely separate populations in genuinely separate knowledge bases.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'An HR corpus contains documents only managers may read. How do you keep the retrieval layer from leaking them to everyone?',
      choices: [
        { id: 'a', text: 'Filter retrieval on entitlement metadata derived from the caller' },
        { id: 'b', text: 'Add a system prompt instruction never to disclose manager-only content', whyWrong: 'By the time the model reads that instruction the restricted chunk is already in its context. You are asking it to un-see the data.' },
        { id: 'c', text: 'Post-process each generated answer to strip out restricted content', whyWrong: 'You are detecting a leak after generation with no reliable way to recognize one, and every miss is a disclosure that already happened.' },
        { id: 'd', text: 'Restrict IAM access to the knowledge base so only managers query it', whyWrong: 'That is all-or-nothing at the knowledge base level. Everyone else loses access to the documents they are perfectly entitled to read.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.kb.runs_out',
    mode: 'drill',
    nodeIds: ['aws.knowledge_bases', 'ai.rerank'],
    difficulty: 'deep',
    explanation:
      'Managed RAG is the right starting point for most corpora, and the honest signals that you have outgrown it are all about the retrieval strategy becoming the product. Document structures the default chunker mangles, retrieval that must combine several sources with custom scoring, and quality targets that need a reranker or query rewriting you control are the usual triggers.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'Which observations genuinely indicate you have outgrown a managed knowledge base? Select all that apply.',
      choices: [
        { id: 'a', text: 'The documents are structured in a way the default chunking mangles, such as long tables split mid-row' },
        { id: 'b', text: 'Answer quality needs query rewriting and a reranker you control and evaluate' },
        { id: 'c', text: 'Retrieval must fuse results from several stores with a custom scoring function' },
        { id: 'd', text: 'The corpus has grown past a few hundred thousand documents', whyWrong: 'Scale is what a managed service handles well. Volume alone is an argument for managed retrieval, not against it.' },
        { id: 'e', text: 'The team wants to see which chunks were used in an answer', whyWrong: 'Citations back to source chunks are part of what the managed path provides.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.kb.retrieve_vs_rag',
    mode: 'drill',
    nodeIds: ['aws.knowledge_bases', 'ai.context'],
    difficulty: 'deep',
    explanation:
      'RetrieveAndGenerate hands you a finished answer with citations, which is the fastest path to a working assistant. Retrieve returns the chunks and leaves generation to you, which is what you need when the prompt has to combine retrieved context with tool results, conversation state or a strict output contract. Reaching for the all-in-one call and then fighting it is a common early mistake.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Your agent has to blend knowledge base results with live API data and emit a strict JSON contract. Which knowledge base API do you use?',
      choices: [
        { id: 'a', text: 'RetrieveAndGenerate, then parse the prose it returns into JSON', whyWrong: 'You would be re-extracting structure from generated text, which is precisely the fragile step a structured output contract exists to remove.' },
        { id: 'b', text: 'Retrieve, so you own the prompt and enforce the schema yourself' },
        { id: 'c', text: 'RetrieveAndGenerate with the API data appended to the question', whyWrong: 'It sometimes works, but you get no control over prompt assembly or output shape, so the strict schema requirement stays unmet.' },
        { id: 'd', text: 'Either one, since both ultimately return the same source chunks', whyWrong: 'One returns chunks and the other returns a generated answer. That difference is exactly what this requirement turns on.' },
      ],
      correctId: 'b',
    },
  },

  // ── Guardrails ───────────────────────────────────────────────────────────
  {
    id: 'aw.gr.enforcement',
    mode: 'drill',
    nodeIds: ['aws.guardrails', 'ai.guardrails'],
    difficulty: 'core',
    explanation:
      'A guardrail evaluates the prompt and the response outside the model and blocks or masks according to a configured policy, so it holds regardless of how the model was talked into misbehaving. A system prompt instruction is input to the same model an attacker is manipulating, which is why "we told it not to" fails a security review the moment injection is raised.',
    citations: cite('awsBedrockGuardrails'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Why is a Bedrock guardrail a stronger control than the same rules written into the system prompt?',
      choices: [
        { id: 'a', text: 'It runs on a larger model that is correspondingly harder to fool', whyWrong: 'The strength comes from being a separate enforcement layer with a configured policy, not from the size of any model behind it.' },
        { id: 'b', text: 'It is applied before the system prompt is assembled and sent', whyWrong: 'Ordering within prompt assembly is beside the point. Anything that lives inside the prompt is subject to the same manipulation.' },
        { id: 'c', text: 'It is enforced outside the model, so no prompt can talk it out of it' },
        { id: 'd', text: 'System prompts are visible to end users, and guardrail rules are not', whyWrong: 'Prompt secrecy is not a control. A perfectly hidden instruction is still only an instruction to a model that can be persuaded.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.gr.apply_api',
    mode: 'drill',
    nodeIds: ['aws.guardrails', 'ai.guardrails'],
    difficulty: 'deep',
    explanation:
      'The ApplyGuardrail API evaluates arbitrary text against a configured guardrail without invoking a model, which lets one policy cover a self-hosted model, a third-party API, or content on its way into a prompt. That last case matters for indirect injection: screening retrieved documents before they enter the context is a control you cannot implement if guardrails only run on model calls.',
    citations: cite('awsBedrockGuardrails'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'One team uses a Bedrock model and another self-hosts, and security wants a single enforced content policy across both. What makes that possible?',
      choices: [
        { id: 'a', text: 'Attach the guardrail directly to the self-hosted model endpoint', whyWrong: 'A guardrail is not attached to arbitrary endpoints. It is invoked as a call, which is exactly what the standalone API exists for.' },
        { id: 'b', text: 'Route the self-hosted model’s traffic through Bedrock as a proxy', whyWrong: 'Bedrock serves the models it offers. It is not a pass-through proxy in front of somebody else’s model deployment.' },
        { id: 'c', text: 'Re-implement the same filters inside the self-hosting team’s app', whyWrong: 'Two implementations drift apart, and the auditor will ask which is authoritative. That is the situation one policy exists to prevent.' },
        { id: 'd', text: 'The ApplyGuardrail API, which evaluates text on its own' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.gr.grounding',
    mode: 'drill',
    nodeIds: ['aws.guardrails', 'ai.rag_failure'],
    difficulty: 'deep',
    explanation:
      'Contextual grounding checks score whether the response is actually supported by the source material provided and whether it is relevant to the question, and let you block responses below a threshold. It targets the specific RAG failure where retrieval returned something adjacent and the model filled the gap confidently, which no content filter would catch because nothing about the text is unsafe.',
    citations: cite('awsBedrockGuardrails'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A RAG assistant occasionally states things the retrieved documents do not support. Which guardrail capability targets that?',
      choices: [
        { id: 'a', text: 'The contextual grounding check, with a block threshold on the score' },
        { id: 'b', text: 'Denied topics, listing the subjects the assistant must refuse', whyWrong: 'Denied topics keep the assistant off subjects you enumerated. An unsupported claim about a permitted subject passes straight through.' },
        { id: 'c', text: 'The sensitive information filter, set to block rather than mask', whyWrong: 'That detects and masks entities such as personal identifiers. It has no opinion on whether a claim is supported by the source.' },
        { id: 'd', text: 'A word filter blocking speculative and hedging phrasing', whyWrong: 'Confident hallucinations rarely announce themselves with hedging words, and blocking those words also removes appropriate uncertainty.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.gr.policy_match',
    mode: 'drill',
    nodeIds: ['aws.guardrails', 'sec.pii'],
    difficulty: 'intro',
    explanation:
      'A guardrail is a bundle of independent policies, and reaching for the wrong one is a common cause of "we configured a guardrail and it did not help". Knowing which policy catches which failure lets you configure the minimum that actually addresses the risk the customer raised.',
    citations: cite('awsBedrockGuardrails'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'match',
      stem: 'Match each Bedrock guardrail policy to the risk it addresses.',
      pairs: [
        { left: 'Content filters', right: 'Harmful categories in the prompt or response' },
        { left: 'Denied topics', right: 'Subjects the assistant must refuse to engage with' },
        { left: 'Sensitive information filters', right: 'Personal identifiers that must be masked or blocked' },
        { left: 'Contextual grounding check', right: 'Answers not supported by the retrieved source' },
      ],
    },
  },
  {
    id: 'aw.gr.limits',
    mode: 'drill',
    nodeIds: ['aws.guardrails', 'ai.tool_calling'],
    difficulty: 'edge',
    explanation:
      'Guardrails screen text going into and out of a model. They do not sit between the model and a tool, so a model persuaded to issue a refund or delete a record is not stopped by any content policy. The control for that lives at the tool: server-side authorization, amount thresholds, idempotency, and a human approval step on the actions that matter.',
    citations: cite('awsBedrockGuardrails'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'An agent can issue refunds. A guardrail is configured on the model. What risk remains?',
      choices: [
        { id: 'a', text: 'None, as long as denied topics covers refund fraud and abuse', whyWrong: 'Denied topics shape the conversation. Nothing about them stands between the model and an API call that moves real money.' },
        { id: 'b', text: 'Guardrails screen text, not actions: the refund tool needs limits' },
        { id: 'c', text: 'Only that the guardrail may block legitimate refund conversations', whyWrong: 'A false positive is an annoyance for a support agent. The unbounded action that moves money is the material risk.' },
        { id: 'd', text: 'That the guardrail evaluation adds latency to each refund call', whyWrong: 'Real but minor, and it costs the customer time rather than money. It is not the risk the security review is about.' },
      ],
      correctId: 'b',
    },
  },

  // ── OpenSearch & vectors ─────────────────────────────────────────────────
  {
    id: 'aw.os.recall_silent',
    mode: 'drill',
    nodeIds: ['aws.opensearch_vectors', 'ai.rag_failure'],
    difficulty: 'deep',
    explanation:
      'Approximate nearest neighbor search trades recall for speed, and the trade is invisible at runtime: no error is raised when the right vector simply was not among the candidates examined. Raising the search-time exploration parameter, or comparing against an exact search on a sample, is how you find out whether recall or generation is at fault.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A document is definitely indexed in OpenSearch but sometimes never appears in retrieval results, with nothing logged. What is the likely mechanism?',
      choices: [
        { id: 'a', text: 'The embedding model wrote a degenerate vector for that document', whyWrong: 'That makes the document consistently unretrievable rather than intermittently missing, and it usually fails loudly at index time.' },
        { id: 'b', text: 'The cluster is under memory pressure and dropping partial results', whyWrong: 'Resource pressure surfaces as latency, rejections or outright errors. It does not quietly return an incomplete result set.' },
        { id: 'c', text: 'Approximate search never considers that vector as a candidate' },
        { id: 'd', text: 'The query is resolving through a stale index alias somewhere', whyWrong: 'Worth ruling out, but an alias pointing at the wrong index produces consistent behavior rather than intermittent misses.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.os.hybrid',
    mode: 'drill',
    nodeIds: ['aws.opensearch_vectors', 'ai.hybrid_search'],
    difficulty: 'core',
    explanation:
      'Dense embeddings capture meaning and are poor at exact tokens: a part number, an error code or a rare acronym has no useful semantic neighborhood. Combining lexical scoring with vector similarity and fusing the results recovers exact-match behavior without losing paraphrase matching, which is why hybrid retrieval is the default recommendation for enterprise corpora.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Support engineers search by exact part numbers and error codes, and the vector-only retriever keeps returning conceptually similar but wrong documents. What do you change?',
      choices: [
        { id: 'a', text: 'Switch to a larger embedding model with more dimensions', whyWrong: 'A bigger embedding still places an arbitrary identifier in semantic space with no meaningful neighbors. The failure is structural.' },
        { id: 'b', text: 'Reduce the chunk size so identifiers dominate their own chunk', whyWrong: 'It shifts the embedding slightly without giving exact-token matching, and it fragments the surrounding context that made the chunk useful.' },
        { id: 'c', text: 'Increase the number of nearest neighbors the retriever returns', whyWrong: 'More approximate neighbors from the wrong region of the space are still the wrong region, and the context window grows for no gain.' },
        { id: 'd', text: 'Move to hybrid retrieval fusing lexical scoring with vectors' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.os.filtering',
    mode: 'drill',
    nodeIds: ['aws.opensearch_vectors', 'ai.rag_failure'],
    difficulty: 'deep',
    explanation:
      'When a metadata filter is applied after approximate search returns its candidates, a narrow filter can eliminate almost all of them and leave you with two results out of a requested ten. The symptom is retrieval that works fine broadly and collapses for a specific tenant or document type. Using the engine’s filter-aware search, or over-fetching before filtering, is the correction.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Retrieval quality is fine overall but terrible when scoped to one small department. What is the mechanism to suspect?',
      choices: [
        { id: 'a', text: 'The filter runs after approximate search and empties the candidates' },
        { id: 'b', text: 'That department’s documents were never embedded and indexed', whyWrong: 'That produces no results at all rather than poor ones, and it would show up immediately as a document count discrepancy.' },
        { id: 'c', text: 'The department writes in jargon the embedding model never saw', whyWrong: 'Plausible for very specialized vocabulary, but it would degrade results gradually rather than producing this sharp filter-scoped cliff.' },
        { id: 'd', text: 'The index has too many segments and needs a force merge', whyWrong: 'Segment merging affects query latency and storage efficiency. It does not change which candidates survive a metadata filter.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.os.sizing',
    mode: 'drill',
    nodeIds: ['aws.opensearch_vectors', 'del.napkin'],
    difficulty: 'deep',
    explanation:
      'A vector index built for low-latency approximate search keeps its graph structure in memory, so the sizing question is driven by vector count times dimensions times bytes per element, plus graph overhead, rather than by disk. That means the honest capacity estimate is done before the index exists, and it is one of the more useful napkin calculations to be able to do in front of a customer.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'What actually drives the cluster size for a low-latency vector search workload?',
      choices: [
        { id: 'a', text: 'Disk capacity for the stored source documents and their fields', whyWrong: 'Disk is cheap and rarely the binding constraint. The approximate search structure has to be memory resident to hit low latency.' },
        { id: 'b', text: 'Memory: vector count times dimensions times bytes, plus overhead' },
        { id: 'c', text: 'Peak query volume, which decides how large the cluster must be', whyWrong: 'Query rate sizes the replica count for throughput. It does not answer whether a single copy of the index fits in memory.' },
        { id: 'd', text: 'The number of metadata fields carried on each indexed document', whyWrong: 'Metadata adds modest storage and mapping cost. It is nowhere near the dominant term in the sizing calculation.' },
      ],
      correctId: 'b',
    },
  },
  {
    id: 'aw.os.debug_order',
    mode: 'drill',
    nodeIds: ['aws.opensearch_vectors', 'ai.evals'],
    difficulty: 'core',
    explanation:
      'When a RAG answer is wrong, the discipline is to walk the pipeline in order rather than reaching for the prompt. Most of the time the fault is upstream of generation, and every minute spent rewriting a prompt to compensate for a chunk that never arrived is wasted.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'order',
      stem: 'Order the checks when a retrieval-augmented answer is wrong.',
      steps: [
        'Confirm the source document exists in the corpus and was ingested',
        'Check the chunk boundaries so the answer text was not split across two chunks',
        'Run the retrieval query alone and inspect whether the right chunk is returned and at what rank',
        'Inspect the assembled context to confirm the chunk actually reached the model',
        'Only then examine the prompt and the generation step',
      ],
    },
  },

  // ── SageMaker ────────────────────────────────────────────────────────────
  {
    id: 'aw.sm.endpoint_cost',
    mode: 'drill',
    nodeIds: ['aws.sagemaker', 'del.tco'],
    difficulty: 'intro',
    explanation:
      'A real-time SageMaker endpoint bills for its provisioned instances continuously, not per request, so a demo endpoint left running over a quarter costs the same as one serving constant traffic. Teams discover this in a cost review months later, and the fix is either autoscaling with a scale-to-zero-capable option such as serverless inference, or a lifecycle policy that tears down idle endpoints.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A data science team has eleven SageMaker real-time endpoints and only two see regular traffic. What is the cost problem?',
      choices: [
        { id: 'a', text: 'Each endpoint is charged per inference request it actually serves', whyWrong: 'Real-time endpoints bill on instance hours. Were it per request, the nine idle endpoints would be free and there would be no problem.' },
        { id: 'b', text: 'Model artifacts sitting in S3 are the dominant line on the bill', whyWrong: 'Artifact storage is trivial next to inference instances that run continuously whether or not anyone calls them.' },
        { id: 'c', text: 'They bill for instance time, so idle endpoints cost like busy ones' },
        { id: 'd', text: 'The endpoints are over-provisioned on memory for their models', whyWrong: 'Right-sizing helps at the margin, but the structural problem is paying for nine endpoints that serve no traffic at all.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.sm.vs_bedrock',
    mode: 'drill',
    nodeIds: ['aws.sagemaker', 'aws.bedrock'],
    difficulty: 'deep',
    explanation:
      'A managed model API removes infrastructure, scaling and patching from the equation and is the right default for general language tasks. SageMaker earns its operational cost when you need a model that is not offered as a managed API, control over the serving stack, custom training on proprietary data, or a classical machine learning model that has nothing to do with foundation models at all.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'When does standing up SageMaker inference beat calling a managed foundation model API?',
      choices: [
        { id: 'a', text: 'When the customer wants a lower effective cost per token served', whyWrong: 'Self-hosting shifts cost to instance hours and engineering time, and it only wins at sustained high utilization. It is not a general cost argument.' },
        { id: 'b', text: 'When the data has to remain inside the customer’s own account', whyWrong: 'A managed model API called within the customer’s account and region does not export their data. This assumption is common and wrong.' },
        { id: 'c', text: 'When the team intends to fine-tune the model on their own data', whyWrong: 'Customization is available on managed paths as well. Wanting to tune a model is not by itself a reason to own an endpoint.' },
        { id: 'd', text: 'When the model, serving stack, or task is not a managed offering' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.sm.async',
    mode: 'drill',
    nodeIds: ['aws.sagemaker', 'scale.queueing'],
    difficulty: 'deep',
    explanation:
      'Spiky inference with a tolerant latency budget is the case asynchronous inference exists for: requests are queued, the endpoint scales with the backlog including down to zero, and the caller is notified when a result is ready. Sizing a real-time endpoint for a peak that occurs twice a day means paying for that peak all day.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Inference requests arrive in bursts twice a day, each payload is large, and a few minutes of latency is acceptable. What serving mode fits?',
      choices: [
        { id: 'a', text: 'Asynchronous inference, which queues and scales to zero between bursts' },
        { id: 'b', text: 'A real-time endpoint provisioned to handle the full burst rate', whyWrong: 'You would pay peak capacity around the clock for a workload that is idle most of the day and has no real-time latency requirement.' },
        { id: 'c', text: 'A real-time endpoint with aggressive autoscaling policies set', whyWrong: 'Autoscaling reacts after load arrives and generally will not reach zero, so you pay a floor and still queue at the head of the burst.' },
        { id: 'd', text: 'A scheduled batch transform job run twice a day over the inputs', whyWrong: 'Reasonable when the inputs are all known in advance, but it does not handle request-driven arrival where callers expect a per-request result.' },
      ],
      correctId: 'a',
    },
  },
  {
    id: 'aw.sm.own_model_cost',
    mode: 'drill',
    nodeIds: ['aws.sagemaker', 'ai.finetune'],
    difficulty: 'deep',
    explanation:
      'The training run is the visible cost and usually the smallest one. Owning a fine-tuned model means owning the labeled data pipeline that keeps it current, the eval set that proves each retrain is not a regression, the serving capacity, and the decision about what to do when a materially better base model ships six months later. Customers should hear all four before they commit.',
    citations: cite('awsWaf'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer wants to fine-tune rather than use a general model with good retrieval. What do you make sure is on the table?',
      choices: [
        { id: 'a', text: 'That fine-tuning reliably beats retrieval on factual accuracy', whyWrong: 'Untrue as a general claim. Fine-tuning teaches form and behavior well, and it is a poor mechanism for injecting facts that keep changing.' },
        { id: 'b', text: 'The standing cost: data upkeep, evals, serving, and base model churn' },
        { id: 'c', text: 'That the training run itself is the main expense to budget for', whyWrong: 'The training job is usually the smallest line of the four. Framing it that way sets the customer up for a bad surprise in month three.' },
        { id: 'd', text: 'That a fine-tuned model cannot be paired with a guardrail policy', whyWrong: 'Guardrails apply independently of how the model was produced, so this is simply false and would misdirect the decision.' },
      ],
      correctId: 'b',
    },
  },

  // ── Model regions & data handling ────────────────────────────────────────
  {
    id: 'aw.res.cross_region',
    mode: 'drill',
    nodeIds: ['aws.bedrock_residency', 'sec.residency'],
    difficulty: 'deep',
    explanation:
      'Cross-region inference improves availability by letting requests be served from other regions within a geography when capacity is tight, which is exactly the behavior a residency commitment forbids. If a customer has committed to processing within one country, that is a design decision to make deliberately, and you have to be able to say which regions are in scope.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'A customer with a strict in-country processing commitment asks whether they can use cross-region inference for better availability. What do you say?',
      choices: [
        { id: 'a', text: 'Yes, because the data is encrypted in transit between the regions', whyWrong: 'Encryption protects confidentiality on the wire. A residency obligation concerns where processing occurs, which encryption does not change.' },
        { id: 'b', text: 'Yes, because only the routing decision leaves the home region', whyWrong: 'The request being routed contains the prompt. There is no routing of the call without the content travelling with it.' },
        { id: 'c', text: 'No, since a request can be served from another region entirely' },
        { id: 'd', text: 'It is fine as long as the results are stored back in their region', whyWrong: 'Residency obligations generally cover processing as well as storage, and that reading will not survive a data protection review.' },
      ],
      correctId: 'c',
    },
  },
  {
    id: 'aw.res.region_choice',
    mode: 'drill',
    nodeIds: ['aws.bedrock_residency', 'del.risk_sequencing'],
    difficulty: 'core',
    explanation:
      'Model availability differs by region and changes over time, so a design that assumes a particular model exists where the customer needs it can fail late and expensively. Confirming availability in the target region before committing to a model, and keeping the integration model-agnostic, turns a project risk into a configuration choice.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'You are scoping a build that must run in a region the customer’s regulator requires. What do you verify before the architecture is signed off?',
      choices: [
        { id: 'a', text: 'That the region offers the compute instance types you had planned on', whyWrong: 'Worth checking, but instance families have substitutes. A model that is not offered in the region has no substitute in that architecture.' },
        { id: 'b', text: 'That latency from the customer’s offices to that region is tolerable', whyWrong: 'Relevant to the user experience, and not the kind of constraint that invalidates an architecture at sign-off.' },
        { id: 'c', text: 'That the region provides at least three availability zones', whyWrong: 'A reasonable resilience check that has nothing to say about whether the AI capability the project depends on exists there.' },
        { id: 'd', text: 'That the models exist in that region, and keep the choice swappable' },
      ],
      correctId: 'd',
    },
  },
  {
    id: 'aw.res.training',
    mode: 'drill',
    nodeIds: ['aws.bedrock_residency', 'sec.soc2'],
    difficulty: 'core',
    explanation:
      'The defensible statements about managed model data handling are the ones written into the service terms and the customer agreement, not the ones a sales deck implies. What you can commit to in a security review is what the contract says, what the region and service configuration enforce, and what the customer themselves controls. Everything else should be sent to legal rather than answered on the call.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'multi',
      stem: 'In a security review, which statements about a managed model service can you responsibly stand behind? Select all that apply.',
      choices: [
        { id: 'a', text: 'The data handling commitments are whatever the service terms and the customer agreement state, and those are the document to point at' },
        { id: 'b', text: 'Whether prompts are retained by your own application logging is entirely under the customer’s control and configuration' },
        { id: 'c', text: 'The region a request is sent to determines where that request is processed, absent an explicitly enabled cross-region feature' },
        { id: 'd', text: 'No employee at the provider can access anything under any circumstances', whyWrong: 'An absolute you cannot verify and do not control. Overclaiming here is how a security review reopens after a lawyer reads the terms.' },
        { id: 'e', text: 'Encryption in transit means residency obligations do not apply', whyWrong: 'Residency concerns where processing happens. Transport encryption is a separate control and does not satisfy it.' },
      ],
      correctIds: ['a', 'b', 'c'],
    },
  },
  {
    id: 'aw.res.logging_leak',
    mode: 'drill',
    nodeIds: ['aws.bedrock_residency', 'sec.pii'],
    difficulty: 'deep',
    explanation:
      'In practice the most likely place a customer prompt ends up somewhere it should not is the customer’s own telemetry: application logs capturing the full request, an observability vendor outside the approved region, or an invocation log bucket with looser access than the source system. The data-flow diagram has to include the logging path, and reviews that skip it miss the real exposure.',
    citations: cite('awsBedrock'),
    origin: 'seed',
    criticScore: null,
    payload: {
      kind: 'mcq',
      stem: 'Where does prompt content most often escape the intended boundary in a real deployment?',
      choices: [
        { id: 'a', text: 'The customer’s own logging and observability path, which frequently captures full prompts and ships them somewhere less controlled' },
        { id: 'b', text: 'The model provider retaining prompts contrary to the agreement', whyWrong: 'A contractual matter that is documented and auditable. It is not where the practical leaks come from.' },
        { id: 'c', text: 'Network interception between the application and the service', whyWrong: 'Transport is encrypted end to end. This is rarely the realistic path.' },
        { id: 'd', text: 'Other tenants of the shared service seeing the prompts', whyWrong: 'Tenant isolation is a core property of the platform. Raising it distracts from the exposure that is actually likely.' },
      ],
      correctId: 'a',
    },
  },
];
